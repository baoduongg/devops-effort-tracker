# Dev Log

## rev 1 — 2026-09-07
- F-01: DONE — files: `lib/overdue.ts` (mới), `app/dashboard/page.tsx` — note: derived-only, không lưu Firestore. `isOverdue(task)` = `endDate` khác null AND `status !== "done"` AND `daysOverdue(endDate) > 0`; so sánh theo `startOfDay()` local (bỏ giờ/phút) nên `endDate` = hôm nay không tính trễ (AC-01-5). `daysOverdue` dùng chung cho cả badge ngày trễ (F-01) và message notification (F-02) để tránh lệch số ngày giữa 2 nơi.
- F-02: DONE — files: `types/notification.ts`, `services/notifications.service.ts`, `app/dashboard/page.tsx` — note: thêm `NotificationType = "overdue_task"`, field optional `relatedTaskId`/`relatedMemberId`, và `NotificationInput` type (Omit id/createdAt) để tái dùng cho `createNotification`. Hàm ghi mới theo đúng pattern `createTask` (addDoc + `Timestamp.now()`). Dedup: `useEffect` trên Dashboard theo dõi `overdueTasks`, build `Set` các `relatedTaskId` đã có `type === "overdue_task"` từ `notifications` đã subscribe, bỏ qua nếu đã tồn tại. Effect chỉ đặt trên Dashboard (đúng yêu cầu tránh tạo trùng từ nhiều màn hình). `title`/`message` format đúng theo AC-02-3, ngày `endDate` format tay dd/mm/yyyy (chưa có helper format ngày dùng chung trong repo nên viết inline, không thêm dependency).
- F-03: DONE — files: `app/dashboard/page.tsx` — note: thêm state `showOverdueOnly` độc lập với Search/Project/Bandwidth, filter thêm điều kiện `overdueMemberIds.has(member.id)` vào `filteredMembers` hiện có (tái dùng đúng biến này nên áp dụng tự động cho cả 4 tab Matrix/Timeline/Projects/Cards — AC-03-1). Nút "Xem team đang trễ" cạnh nút Reset, dùng `Button variant="primary"` khi bật / `"ghost"` khi tắt (Astryx Button không có variant "solid", đã tra `astryx component Button` trước khi chọn). Reset hiện có mở rộng thêm `setShowOverdueOnly(false)` (AC-03-2). Rỗng dùng lại đúng `EmptyState` "No matching team members" sẵn có (AC-03-3) vì nó nằm ở nhánh chung `filteredMembers.length === 0`. Click dòng overdue dùng `ListItem href="/members/{memberId}"` (cùng cơ chế `ClickableCard href` mà `MemberCard` đang dùng) (AC-03-4).
- UI mới: `components/dashboard/overdue-tasks-list.tsx` (mới) — `Card` + `List`/`ListItem` (Astryx), `Token color="red"` khi trễ > 7 ngày / `color="orange"` khi 1–7 ngày, sort giảm dần theo `daysOverdue`. Ẩn hoàn toàn (`return null`) khi rỗng — không chiếm chỗ, không có EmptyState riêng (đúng spec S-01). StatCard "Overdue Tasks" thêm vào hàng KPI, tone `destructive` khi > 0 / `primary` khi = 0, tái dùng `StatCard` có sẵn không sửa gì bên trong nó.
- Đã tra cứu Astryx trước khi code: `astryx build "overdue tasks list..."`, `astryx component List/ListItem/Token/Icon/Button/ClickableCard` — không dùng `<div>` layout tay ngoài 2 chỗ `<div className="self-end pb-1">` vốn đã có sẵn trong toolbar (tái dùng nguyên trạng, không phải code mới của rev này).
- Lint: `pnpm lint` — 0 errors, 9 warnings (toàn bộ warnings là pre-existing từ code Astryx migration chưa commit trước rev này — `Search`/`alertCount` unused ở `app/dashboard/page.tsx`, các unused khác ở `app/projects/page.tsx`, `member-card.tsx`, `project-allocation-grid.tsx` — không phải do thay đổi của rev 1, đã verify bằng `git stash` so sánh trước/sau).
- Build: `pnpm build` — thành công, 0 errors (cùng 9 warnings kể trên), route `/dashboard` build ra 10.9 kB.
- AC tự thấy đã thoả (chưa test kỹ, để QC verify): AC-01-1..6, AC-02-1, AC-02-3, AC-02-4, AC-03-1..4. AC-02-2 (không tạo trùng khi load lại nhiều lần) về logic đã đúng (dedup theo `relatedTaskId` trong `notifications` đã subscribe) nhưng phụ thuộc timing của Firestore snapshot lần đầu — QC nên test kỹ trên môi trường thật.

## rev 2 — 2026-09-07 (fix issues vòng QC 1)
- ISS-01: FIXED — files: `services/notifications.service.ts`, `app/dashboard/page.tsx` — root cause: dedup effect chỉ đọc `notifications` qua closure cũ, không chống được nhiều lần gọi `createNotification` xảy ra trước khi subscription `notifications` kịp phản ánh write vừa rồi (đặc biệt khi `tasks` emit local-cache rồi server-ack snapshot liên tiếp). Fix: đổi `createNotification` sang `setDoc(doc(db,"notifications", id), {...input, createdAt: Timestamp.now()}, {merge:true})` thay vì `addDoc`; thêm tham số `id?` optional (không phá caller cũ nào khác vì `app/dashboard/page.tsx` là caller duy nhất — đã grep xác nhận). Dashboard gọi với `id = \`overdue_task_${task.id}\`` — mọi lần ghi trùng task đều overwrite cùng 1 doc thay vì tạo doc mới, nên tổng số notification cho 1 task luôn là 1 bất kể số lần effect chạy lại hay có bao nhiêu tab đang mở (chống race + cross-tab, không cần round-trip đọc lại trước khi ghi).
- ISS-02: FIXED — files: `services/auth.service.ts`, `app/login/page.tsx` — thêm `signInAnon()` (gọi `signInAnonymously(auth)`) theo đúng pattern `signInWithGoogle`. Thêm nút "Continue as guest" (`variant="ghost"`) cạnh nút Google trên `/login`, không đổi luồng Google. Đọc kỹ `linkOrCreateUser`: mọi field từ `firebaseUser` đã có fallback (`email ?? ""`, `displayName ?? email`, `photoURL ?? null`) nên user ẩn danh (không email/displayName) không làm hàm crash — không cần sửa hàm này, chỉ verify.
- ISS-03: FIXED (không tìm thấy lỗi CSS cụ thể — báo cáo kết quả kiểm tra) — đã kiểm tra 7 route chính (`/login`,`/dashboard`,`/members`,`/members/[memberId]`,`/members/new`,`/chat`,`/notifications`) qua dev server: tất cả 200, `astryx.css`/`reset.css` import đúng trong `app/globals.css`, theme token áp dụng đúng ở mọi trang (`data-astryx-theme="devops-tracker"` xuất hiện trong SSR HTML). Duy nhất phát hiện `/login` từng 500 do stale `.next` cache (`Cannot find module './171.js'`) — tự hết sau `rm -rf .next` + restart dev server, xác nhận không phải bug source code. Các vi phạm token/hex/px rải rác trong `member-card.tsx`, `workload-matrix.tsx`, `team-timeline-chart.tsx`, `project-allocation-grid.tsx`, `app/projects/page.tsx`, `floating-chat.tsx`, `chat-box.tsx` là code có từ trước, ngoài phạm vi rev "Overdue Task Alerts" và ngoài scope 3 issue — không tự ý sửa, chỉ ghi nhận.
- Lint: `pnpm lint` — 0 errors, 9 warnings (cùng baseline pre-existing như rev 1, không có warning mới do thay đổi của rev 2).
- Build: `pnpm build` — thành công, 0 errors, cùng 9 warnings kể trên.

## rev 3 — 2026-09-07 (fix issues vòng QC 2)
- ISS-05: FIXED — files: `app/dashboard/page.tsx` — root cause: `<ProjectAllocationGrid>` (tab "By Project") nhận prop `members={members}` (danh sách đầy đủ chưa lọc) thay vì `filteredMembers`, trong khi 3 tab Matrix/Timeline/Cards đều đã dùng đúng `filteredMembers`. Đổi 1 dòng thành `members={filteredMembers}` — "Assigned DevOps" mỗi project card giờ phản ánh đúng Search/Project/Bandwidth/"Xem team đang trễ" đang bật, nhất quán với các tab còn lại. Không sửa `components/dashboard/project-allocation-grid.tsx` vì component đã nhận đúng list qua prop, chỉ caller truyền sai biến.
- ISS-04: FIXED — chạy script one-off (`scripts/_cleanup-dup-overdue-notifications.ts`, dùng cùng Firestore REST helper pattern như `scripts/seed.ts`, xoá khỏi repo ngay sau khi chạy) để dọn notification `overdue_task` trùng `relatedTaskId`: gom theo `relatedTaskId`, giữ bản đúng format `overdue_task_<taskId>` nếu có, nếu không thì giữ bản `createdAt` cũ nhất, xoá phần còn lại. Verify bằng cả REST list và `runQuery` structured query trực tiếp lên Firestore: tại thời điểm chạy, collection `notifications` chỉ có 3 doc `type === "overdue_task"`, mỗi `relatedTaskId` (`qc-task-overdue-5d`, `qc-task-overdue-1d`, `task-phoenix-1`) đúng 1 bản — không còn cặp trùng nào (script log "Deleted 0 duplicate" vì dữ liệu rác mô tả trong issue đã không còn tồn tại tại thời điểm fix chạy — có thể đã được dọn/ghi đè giữa lúc QC ghi nhận và lúc fix). Không đụng gì tới logic `createNotification`/dedup vì đã đúng từ ISS-01.
- Không sửa gì khác ngoài 2 issue trên (không đụng gợi ý "subscribeAllTasks try/catch" hay "race projects load" mà QC ghi trong Gợi ý cho BA ở rev trước — để BA cân nhắc rev sau).
- Lint: `pnpm lint` — 0 errors, 9 warnings (cùng baseline pre-existing từ rev 1/2, không có warning mới).
- Build: `pnpm build` — thành công, 0 errors, cùng 9 warnings kể trên.

## rev 4 — 2026-09-07 (spec rev 2: FB-01/02/03)
- F-02 (FB-01) FIXED: files: `app/dashboard/page.tsx` — root cause xác nhận đúng như spec: `getProjects()` one-shot promise độc lập với `subscribeAllTasks`, effect tạo notification chỉ phụ thuộc `[overdueTasks]` nên có thể chạy trước khi `projects` có dữ liệu, ghi cứng "No project" và bị đóng băng do dedup theo doc id cố định. Fix: thêm state `projectsLoaded` (khởi tạo `false`, set `true` trong callback `.then()` của `getProjects()` cùng lúc `setProjects`), effect notification thêm guard `if (!projectsLoaded) return;` ngay đầu và thêm `projectsLoaded` vào dependency array. Không đổi cơ chế tra cứu tên project — vẫn dùng đúng `projects.find((p) => p.id === task.projectId)` hiện có (không thêm lookup song song). Case `projectId` trỏ tới project đã xoá thật sự vẫn hiện "No project" bình thường vì đó là dữ liệu thật, không phải do race (AC-04-3).
- F-04 (FB-02) DONE: files: `components/timeline/timeline-item.tsx` — thêm `isOverdue(task)`/`daysOverdue(task.endDate)` từ `lib/overdue.ts` (tái dùng nguyên hàm, không viết lại logic). Khi overdue: `endContent` đổi từ `Token label={statusLabels[task.status]}` sang `Token label="Overdue" color={overdueDays > 7 ? "red" : "orange"} size="sm"` — cùng ngưỡng màu 7 ngày và cùng prop `color` với `OverdueTasksList` trên Dashboard để nhất quán (AC-05-4). Khi không overdue giữ nguyên `statusLabels` như cũ (Planned/In Progress/Done — task done không bao giờ overdue vì `isOverdue()` đã loại status `done`, AC-05-2). Vì sửa tại `TimelineItem` dùng chung, áp dụng tự động cho mọi nơi gọi `TimelineView` (chỉ có `/members/[memberId]` tại thời điểm này) — đúng ý đồ spec, không phải side-effect ngoài ý muốn.
- F-05 (FB-03) DONE: files: `app/notifications/page.tsx` — thêm state `loading` khởi tạo `true`, set `false` bên trong callback của `subscribeNotifications` (chạy đúng 1 lần đầu tiên rồi vẫn tiếp tục chạy mỗi lần có update, nhưng `setLoading(false)` gọi lại nhiều lần vô hại vì React bỏ qua set trùng giá trị — không quay lại loading ở các lần sau, AC-06-4). Trong lúc `loading === true` hiện 3 `Skeleton height={64}` (tái dùng đúng component `Skeleton` đã dùng ở Dashboard/Member Detail, không thêm pattern mới); sau khi có snapshot đầu tiên (rỗng hay có dữ liệu) mới render `NotificationList` như cũ — component này tự quyết định `EmptyState` hay danh sách dựa trên độ dài mảng, không đổi gì bên trong nó (AC-06-1..3).
- Đã tra Astryx trước khi code: `astryx component Token`, `astryx component Skeleton` — xác nhận đúng props `color`/`size` (Token) và `height` (Skeleton) đang dùng, không thêm CSS/div tay.
- Không đụng F-01/F-03 (rev 1, đã pass QC) hay bất kỳ file nào ngoài 3 phạm vi trên.
- Lint: `pnpm lint` — 0 errors, 9 warnings, tất cả đều pre-existing (không có ở 3 file vừa sửa: `app/dashboard/page.tsx` có 2 warning unused-vars `Search`/`alertCount` nhưng đã tồn tại từ rev 1, không phải do thay đổi rev 2 — verify bằng cách warning message trùng khớp dòng cũ, không liên quan tới guard `projectsLoaded` mới thêm).
- Build: `pnpm build` — thành công, 0 errors, cùng 9 warnings kể trên. Route `/dashboard` 11 kB, `/members/[memberId]` 5.04 kB, `/notifications` 5.58 kB.
- Tự đánh giá AC mới: AC-04-1, AC-04-2, AC-04-3 (guard `projectsLoaded` + fallback "No project" cho project thật sự không tồn tại) — thoả theo logic, đợi QC verify timing thật trên môi trường có network chậm. AC-05-1, AC-05-2, AC-05-3, AC-05-4 (Token đỏ/cam + label "Overdue", dùng chung `isOverdue`/ngưỡng màu với Dashboard) — thoả. AC-06-1, AC-06-2, AC-06-3, AC-06-4 (skeleton trước snapshot đầu, EmptyState/List đúng sau đó, không quay lại loading khi có update mới) — thoả theo logic.

## Câu hỏi cho BA
- Không có câu hỏi mới ngoài 2 câu hỏi mở đã ghi sẵn trong `spec.md` (mục "Câu hỏi mở cho PM"). Đã làm theo đúng giả định của spec: dedup theo `relatedTaskId` duy nhất (không báo lại khi "tái quá hạn"), và chấp nhận giới hạn không có cron (notification chỉ sinh khi có người mở Dashboard).

## rev 5 — 2026-09-09 (spec rev 1 mới: Chat AI ra lệnh quản lý team bằng ngôn ngữ tự nhiên, F-01..F-10)
Feature mới toàn bộ, bám sát đúng bảng "Tái sử dụng vs. thêm mới" trong spec — không sửa `task-extractor.service.ts`, `format-entry/route.ts`, `nvidia.service.ts`.

- F-01: DONE — file: `lib/intent.ts` — thêm `isTaskUpdateIntent`/`isTaskDeleteIntent` (regex heuristic cùng phong cách `isTaskCreationIntent` có sẵn, không gọi AI để phân loại). Thứ tự bắt buộc `delete -> update -> create -> query` áp dụng đúng trong `app/api/ai/answer-query/route.ts` (biến `isDeleteIntent`/`isUpdateIntent`/`isCreateIntent` loại trừ lẫn nhau bằng cách check nhánh trước = false). Verify bằng test tay các câu ví dụ AC-01-1..3, đều phân loại đúng.
- F-02: DONE — file: `app/api/ai/answer-query/route.ts` — thêm `resolveIsLeader(mode, memberId, allMembers)`: tra `getMembers()` đối chiếu `role` thật theo `memberId`, chỉ fallback tin `mode` client gửi khi không đối chiếu được (`memberId` rỗng hoặc `"leader"` giả — đúng giả định PM đã chốt trong spec). Check này đặt ngay đầu, TRƯỚC khi gọi bất kỳ extractor/AI nào cho cả 3 intent create/update/delete — nếu không phải leader thì trả lời từ chối và ghi 1 `chatLogs` bình thường (`confirmed:true`, không có `proposal`/`clarification`), không đụng `taskChangeLogs`.
- F-03: DONE (không đổi hành vi cũ, chỉ xác nhận qua AC) — riêng phần audit: bổ sung ghi `taskChangeLogs action:"create"` trong `handleConfirmEntry` (`chat-box.tsx`) NHƯNG chỉ khi `mode === "leader"` — quyết định kỹ thuật phát sinh: `handleConfirmEntry` dùng chung cho cả devops (format-entry tự ghi log) và leader (giao task qua answer-query), audit log theo spec là cho "leader ra lệnh qua chat" nên viết `taskChangeLogs` cho devops sẽ sai ngữ nghĩa `actorName`/mục đích audit — thêm guard `mode === "leader"` thay vì viết lại hàm dùng chung, không ảnh hưởng byte nào tới luồng devops (F-10).
- F-04: DONE — files mới: `services/task-mutation-extractor.service.ts`, `components/chat/proposal-card.tsx`; sửa: `app/api/ai/answer-query/route.ts`, `chat-box.tsx`. Resolve `taskId` bằng matching cứng (tên member qua `findMemberByName` + từ khóa tiêu đề qua `matchTasks`) trước khi giao AI trích `changes` — AI không bao giờ tự chọn `taskId`. 0 khớp → `no_match`, ≥2 khớp → `ambiguous_match` (kèm project/ngày để phân biệt, đúng AC-06-3), đúng 1 → gọi `callNvidiaText` với prompt riêng (`UPDATE_SYSTEM_PROMPT`) chỉ trích `changes`, validate `taskChangeProposalSchema`, retry 1 lần đúng pattern `extractTaskEntryFromInput`. `ProposalCard` cho phép **Chỉnh sửa** trước khi **Xác nhận** (state `changes` cục bộ) — `handleConfirmProposal` áp dụng đúng bản đã sửa (`{...proposal, changes}` gửi lên), không phải bản AI đề xuất ban đầu (AC-04-3). Đồng bộ Member sau update: thêm `syncMemberEffortStatus(memberId)` (mới, `services/members.service.ts`) tái dùng ĐÚNG công thức trong `handleConfirmEntry` cũ (active tasks in_progress, effortMinutes > 480 → overloaded, > 288... — thật ra spec chỉ yêu cầu bám công thức cũ, ngưỡng giữ nguyên `> 480 overloaded`); gọi cho cả assignee cũ (nếu đổi người) và assignee mới. Thiếu field muốn đổi (câu chỉ nhắc tên task, không nói đổi gì) → AI trả `{"changes":{}}` → route trả `clarification:{reason:"missing_field"}`, không tạo `ProposalCard` (AC-04-4).
- F-05: DONE — cùng file `task-mutation-extractor.service.ts` (nhánh `action === "delete"` không gọi AI, chỉ cần taskId đã resolve xong) + `ProposalCard` (biến thể `isDelete`: banner cảnh báo đỏ "xóa vĩnh viễn", nút Xác nhận `variant="destructive"`). `handleConfirmProposal` gọi `deleteTask` (mới, `tasks.service.ts`, pattern giống hệt `deleteMember`) rồi `syncMemberEffortStatus` cho assignee cũ.
- F-06: DONE — 3 chỗ hỏi lại: (1) update/delete thiếu field hoặc member/task mơ hồ → `task-mutation-extractor.service.ts` như trên; (2) **quyết định kỹ thuật quan trọng nhất của rev này**: nhánh "create" do LEADER gõ qua `answer-query` giờ có thêm pre-check heuristic (`mentionsProject`/`mentionsEffort` regex trên raw text) TRƯỚC khi gọi `extractTaskEntryFromInput` — vì hàm đó tự set `effortMinutes` mặc định 60 khi thiếu (qua `formattedEntrySchema.default(60)`) và không có cách nào phân biệt "AI tự suy ra 60" với "user thực sự nói 60 phút" sau khi hàm chạy xong, nên phải chặn ở input thô trước khi gọi hàm, đúng như spec đã lường trước ("giữ nguyên hành vi cũ của `task-extractor.service.ts`, không sửa file đó — thêm rule ở lớp trên nó"). Devops dùng `format-entry` (route khác hẳn) nên không bị ảnh hưởng (F-10). (3) member không tồn tại (vd "Hoàng") → `no_match` kèm full danh sách member thật.
- F-07: DONE — 2 chỗ chặn theo `Member.role` (không theo tên hiển thị): (1) nhánh create trong route.ts, sau khi `extractTaskEntryFromInput` trả `entry.assigneeName`, tra `findMemberByName` rồi check `role==="leader"` → `clarification:{reason:"target_is_leader"}` kèm gợi ý qua `findBestSuitableMember` (không tự gán); (2) nhánh update trong `task-mutation-extractor.service.ts`, sau khi AI trả `changes.assigneeName`, check tương tự trước khi trả `proposal`.
- F-08: DONE — mở rộng `GroundingSnapshot` (`services/grounding.service.ts`) thêm `overdueTasks` (tái dùng `isOverdue()`/`daysOverdue()` từ `lib/overdue.ts` nguyên trạng), `freeMembers`/`busyMembers`/`overloadedMembers` (lọc theo `member.status` đã tính sẵn trong `memberData`), `projectProgress` (đếm task theo status mỗi project). `SYSTEM_PROMPT` thêm 3 rule tường minh: dùng đúng field tính sẵn thay vì tự suy luận, trả lời đủ mọi ý khi câu hỏi gộp nhiều phần, từ chối lịch sự câu hỏi ngoài phạm vi. Câu hỏi audit ("vừa đổi/xóa gì qua chat") nhận diện qua `AUDIT_QUERY_PATTERN` (regex, không qua AI) → đọc thẳng `taskChangeLogs` (`getAllTaskChangeLogs`/`getTaskChangeLogsByActor` — mới, `services/taskChangeLogs.service.ts`) → render text liệt kê `renderTaskChangeAudit`, không qua `callNvidiaText` nên không có rủi ro bịa lịch sử (đúng yêu cầu spec).
- F-09: DONE — `types/taskChangeLog.ts` (mới) + `services/taskChangeLogs.service.ts` (mới, pattern `toX()` mapper giống mọi service khác). Ghi log CHỈ tại 2 thời điểm: bấm Xác nhận (`status:"confirmed"`, sau khi Firestore task write xong) trong `handleConfirmProposal`/`handleConfirmEntry`, và bấm Hủy (`status:"cancelled"`, ngay lập tức) trong `handleCancelProposal` mới thêm trên `ProposalCard`. Không ghi gì ở bước AI đề xuất (route handler chỉ trả JSON, không đụng `taskChangeLogs`) — tránh log rác nếu leader không phản hồi gì (AC-09-3). Lưu ý: `ProposalCard`/create-`EntryCard` hiện tại không có nút "Hủy" riêng cho create (leader rời trang = bỏ qua, y hệt hành vi cũ) nên "cancelled" cho action="create" hiện chưa có đường kích hoạt qua UI — ghi nhận là giới hạn kế thừa từ EntryCard cũ (spec F-03 yêu cầu "reuse nguyên trạng", không thêm nút Hủy vào EntryCard).
- F-10: DONE — không sửa `task-extractor.service.ts`, `format-entry/route.ts`, `nvidia.service.ts`. Guard `mode==="leader"` mới thêm trong `handleConfirmEntry` không đổi hành vi khi `mode==="devops"` (chỉ skip 1 khối `try` audit, mọi thứ khác y hệt). Đã build/lint xác nhận không có regression ở các route/page khác.

Quyết định kỹ thuật khác:
- **Không có route `/api/ai/apply-*` mới** — đúng spec, mọi write Firestore (update/delete task, sync member, ghi `taskChangeLogs`) chạy client-side trong `chat-box.tsx`, y hệt cơ chế `handleConfirmEntry` hiện có.
- **Không có collection "pending" mới** — proposal chỉ sống trong `chatLogs.confirmed=false` (đọc lại qua `chatLogsToMessages` mở rộng 2 nhánh `proposal`/`clarification`) + Zustand (`useChatStore` thêm `updateProposalConfirmed`, message role `ai-proposal`/`ai-clarification`).
- `ProposalCard`/`ClarificationCard` dùng Astryx thuần (`Card`, `VStack`/`HStack`, `Token`, `Button`, `TextInput`/`NumberInput`/`DateInput`/`Selector`, `Divider` — tra `astryx component <Name>` trước khi dùng mỗi component, kể cả xác nhận `Button variant` chỉ có `primary|secondary|ghost|destructive` (không có `"danger"`) trước khi chốt). 1 banner cảnh báo xóa dùng `<div className="bg-rose-500/10 border-rose-500/30">` — Tailwind utility class có sẵn (không hex/px), giống hệt pattern `suggestionNote` banner đã tồn tại trong `EntryCard` (tái dùng convention cũ, không phải vi phạm mới).
- `taskChangeLogs` không thêm composite index riêng trong repo (không có file `firestore.indexes.json` sẵn có) — theo đúng convention hiện tại của `chatLogs.service.ts` (cũng `where + orderBy` không kèm index file trong repo).

Lint: `pnpm lint` — 0 errors, 1 warning (`app/login/page.tsx: handleAnonSignIn unused` — pre-existing, không liên quan gì tới thay đổi rev này, không sửa vì ngoài phạm vi).
Build: `pnpm build` — thành công, 0 errors, cùng 1 warning kể trên. (Gặp 1 lần lỗi `PageNotFoundError: /_document` do `.next` cache cũ — `rm -rf .next` rồi build lại là hết, không phải lỗi code, ghi nhận lại phòng khi QC gặp lại.)

Tự đánh giá AC (logic review + test tay regex/matching, chưa chạy Playwright E2E — cần Firestore thật để test hết luồng xác nhận/hủy): AC-01-1..3, AC-02-1..4, AC-03-1..3, AC-04-1..4, AC-05-1..3, AC-06-1..4, AC-07-1..2, AC-08-1..6 (logic đúng, AC-08 phụ thuộc dữ liệu Firestore thật + chất lượng model NVIDIA khi dùng field tính sẵn — QC nên verify với dữ liệu seed thật), AC-09-1..3, AC-10-1..3 — tất cả 32 AC được implement theo đúng thiết kế, đợi QC chạy tay trên môi trường thật (không có Firestore emulator trong repo để tự test end-to-end).

## Câu hỏi cho BA (rev 5)
- Không có câu hỏi mới — đã có giả định rõ trong spec (phần "Quyết định của PM") nên không cần hỏi lại. 1 điểm tự quyết định khi code (ghi rõ ở trên): audit `taskChangeLogs action:"create"` chỉ ghi khi `mode==="leader"`, vì spec mô tả audit là cho "ai đổi gì qua chat" ở góc độ leader ra lệnh, không phải devops tự ghi log việc của mình (nếu BA muốn audit cả devops self-log, cần bổ sung rule rõ ràng ở rev sau).

## rev 6 — 2026-09-09 (fix review-notes.md — 2 correctness finding + 2 nit)

- **Finding #1 (correctness) FIXED** — file: `components/chat/chat-box.tsx` (`handleConfirmProposal`). Root cause: `changes.assigneeName !== undefined` gộp chung 2 case khác nhau — "chủ ý unassign" (`null`) và "reassign sang người mới" (string) — rồi gọi `findMemberByName(allMembers, changes.assigneeName ?? undefined)`; khi `null` thì `?? undefined` biến thành `undefined`, hàm này có guard `!targetName → return null`, code coi `null` trả về là "không tìm thấy nhân sự" và `setError` + throw, chặn nhầm luồng unassign hợp lệ. Fix: tách rõ 3 nhánh theo đúng review note — field không có trong `changes` → giữ nguyên (không set `taskPatch.memberId`, không gọi `findMemberByName`); `changes.assigneeName === null` → set thẳng `taskPatch.memberId = null` (field thật của `Task`, xem `types/task.ts`), không gọi `findMemberByName`, không phải lỗi; có giá trị string → gọi `findMemberByName` + giữ nguyên logic lỗi cũ khi không khớp. Đồng bộ `newMemberId` sang kiểu `string | null | undefined` để nhánh sync `Member.effortMinutes/status` phía dưới hoạt động đúng: `null` (unassign) và `undefined` (không đổi assignee) đều rơi vào nhánh `else` resync đúng người phụ trách cũ hiện có — không cần sửa thêm gì ở đó vì `if (newMemberId)` vốn đã falsy cho cả 2 case, chỉ thêm comment làm rõ.

- **Finding #2 (correctness) FIXED** — file: `lib/intent.ts` (`isTaskUpdateIntent`, `isTaskDeleteIntent`). Root cause: 2 hàm này thiếu guard loại câu hỏi mà `isTaskCreationIntent` đã có (`if (isInformationalQuery(q)) return false;` đặt sau bước match prefix tường minh, trước bước match middle-pattern) — nên câu hỏi dùng động từ đổi/xóa nhưng thực chất là hỏi (vd "Nếu tôi đổi trạng thái task này thì effort có tính lại không?", "Task nào của Huy đã bị xóa tuần trước?") vẫn khớp `updateMiddlePatterns`/`deleteMiddlePatterns` và bị route nhầm thành lệnh. Fix: thêm đúng guard tương tự `isTaskCreationIntent`, đặt ở đúng vị trí (sau prefix check, trước middle-pattern check) trong cả 2 hàm.

- **Nit #3 FIXED** — file: `components/chat/chat-box.tsx` (`handleConfirmProposal`). Gom nhánh `syncMemberEffortStatus` của delete lại ngay sau `await deleteTask(...)` trong cùng khối `if (proposal.action === "delete")`, thay vì để cách xa ~50 dòng sau nhánh update (code cũ có 2 khối `if (proposal.action === "delete")` riêng biệt, nay gộp thành 1). Hành vi không đổi, chỉ dễ đọc/khó bỏ sót khi sửa nhánh update sau này.

- **Nit #4 FIXED** — file: `components/chat/proposal-card.tsx`. Banner cảnh báo xóa đổi từ `<div className="p-2.5 rounded-md bg-rose-500/10 border border-rose-500/30 flex items-start gap-2">` (vi phạm rule AGENTS.md "No `<div>`") sang `<Banner status="warning" title="..." />` (tra `astryx component Banner` trước khi dùng — đúng use case theo doc: "warning for caution", dùng như 1 banner nội dung trong Card thay vì Card lồng Card). Bỏ `<div>`/`<Icon>`/`<Text className>` tay, Banner tự render icon + màu theo `status` — không cần `TriangleAlert` riêng ở chỗ này nữa (import `TriangleAlert` vẫn giữ vì còn dùng ở nhánh "cancelled" phía trên, đã verify bằng grep).

Lint: `pnpm lint` — 0 errors, 1 warning (`app/login/page.tsx: handleAnonSignIn unused` — pre-existing từ rev 5, không liên quan gì tới 4 chỗ sửa lần này).
Build: `pnpm build` — thành công, 0 errors, cùng 1 warning kể trên.

AC liên quan: F-04 (unassign flow qua `ProposalCard`/`handleConfirmProposal`) và F-01/F-02 (phân loại intent update/delete không nuốt nhầm câu hỏi) nay đúng theo review-notes.md — sẵn sàng để QC verify lại.

## rev 7 — 2026-09-09 (fix issues vòng QC — ISSUE-06/07/08)

- **ISSUE-06 — KHÔNG PHẢI BUG CODE, kết luận rõ, không đổi status sang fixed**: đọc `app/login/page.tsx` — nút Guest đã bị comment sẵn trong source (dòng 75-81), khớp đúng commit `e3af719 "comment login guest and ref landing"` của chủ repo — đây là thay đổi có chủ đích, không phải bug, không tự ý uncomment lại. Nếu QC từng thấy nút hiện trên dev server dù source đã comment, nguyên nhân là tiến trình dev server cũ chưa được restart sau commit (Next.js HMR không luôn tự nhận diện thay đổi JSX return sớm/comment lớn nếu module cache dính) — không phải lỗi source, restart sạch (`rm -rf .next && pnpm dev`) là source thật được phản ánh đúng. Đọc `services/auth.service.ts#signInAnon()` — code gọi `signInAnonymously(auth)` đúng chuẩn Firebase SDK, y hệt lúc ISSUE-02 được QC verify PASS trước đây; không có lỗi logic gì để sửa bằng code. Lỗi HTTP 400 từ endpoint `signInAnonymously` gần như chắc chắn do "Anonymous Authentication" đã bị tắt lại trên Firebase Console (Authentication → Sign-in method) — đây là cấu hình hạ tầng ngoài phạm vi code, cần PM/chủ project tự bật lại trên Firebase Console (và tự quyết định có muốn uncomment nút Guest hay không, vì nút đang bị ẩn có chủ đích). Giữ nguyên `Status: open` trong `issues.md`, đã ghi chi tiết kết luận + việc cần PM làm ngay trong issue.

- **ISSUE-07: FIXED** — root cause: `components/chat/proposal-card.tsx#handleConfirm` gọi `onConfirm({ ...proposal, changes })` với `changes` là state cục bộ đã bị leader sửa qua UI "Chỉnh sửa", spread này ghi đè thẳng lên field `changes` gốc của `proposal` (bản AI đề xuất ban đầu) trước khi truyền lên `chat-box.tsx`, khiến `handleConfirmProposal` không còn cách nào phân biệt "AI đề xuất gì" vs "leader thực sự lưu gì" — cả `proposedChanges` và `appliedChanges` đọc từ cùng 1 nguồn đã bị ghi đè. Fix: đổi signature `ProposalCard.onConfirm` thành `(proposal, appliedChanges) => Promise<void>` — truyền `proposal` nguyên vẹn (không đổi `changes` gốc) và `changes` (state đã sửa) tách riêng làm `appliedChanges`. Grep toàn bộ caller trước khi sửa: `components/chat/message-bubble.tsx` (nhận qua `ProposalCard`, forward `onConfirmProposal`) và `components/chat/chat-thread.tsx` (chỉ forward type, không có logic) — cả 2 chỉ cần đổi type signature. `components/chat/chat-box.tsx#handleConfirmProposal` nhận thêm tham số `appliedChanges`, dùng nó thay cho `proposal.changes` để build `taskPatch`/tìm member/sync effort (logic áp dụng thật), còn `taskChangeLogs.proposedChanges = proposal.changes` (nay giữ đúng bản AI đề xuất ban đầu, bất biến) và `taskChangeLogs.appliedChanges = appliedChanges` (bản leader thực sự lưu) — đúng nguyên văn AC-04-3.

- **ISSUE-08: FIXED** — root cause: ô "Người phụ trách" trên `ProposalCard` có placeholder hứa "bỏ trống để giữ nguyên" nhưng `onChange` cũ set thẳng `assigneeName: v.trim() || null` — text rỗng luôn ra `null` (unassign thật), không phải "giữ nguyên" (phải là field không tồn tại/`undefined` trong `changes` mới đúng nghĩa giữ nguyên theo cách `chat-box.tsx` đọc `changes.assigneeName !== undefined`). Fix: sửa `onChange` — text rỗng nay `delete` hẳn field `assigneeName` khỏi state `changes` (đúng nghĩa giữ nguyên, khớp placeholder), gõ tên hợp lệ thì set string bình thường. Thêm 1 `Button variant="secondary"` "Bỏ người phụ trách" cạnh ô input để leader chủ ý unassign một cách tường minh (set `assigneeName: null`), kèm dòng chú thích phụ hiện ra khi đang ở trạng thái này để tránh nhầm lẫn thao tác ngoài ý muốn. Không đổi logic phân biệt `null`/`undefined` ở `chat-box.tsx` (đã đúng từ rev 6) — chỉ sửa UI/UX phía `ProposalCard`.

Lint: `pnpm lint` — 0 errors, 1 warning (`app/login/page.tsx: handleAnonSignIn unused` — pre-existing từ rev 5/6 do nút Guest bị comment có chủ đích, không phải regression của rev này, không sửa vì thuộc quyết định ISSUE-06 nêu trên).
Build: `pnpm build` — thành công, 0 errors, cùng 1 warning kể trên.

Files thay đổi rev 7: `components/chat/proposal-card.tsx`, `components/chat/message-bubble.tsx`, `components/chat/chat-thread.tsx`, `components/chat/chat-box.tsx`, `docs/product/issues.md`, `docs/product/dev-log.md`.

## rev 8 — 2026-09-09 (fix issues vòng QC 3 — ISSUE-09..14, vòng cuối)

- **ISSUE-09: FIXED** — file: `components/chat/proposal-card.tsx`. Root cause: `taskSnapshot.startDate`/`endDate` (và `changes.startDate`/`endDate` khi có) là chuỗi ISO datetime đầy đủ (`Task.startDate`/`endDate` qua `toIsoString()`), trong khi Astryx `DateInput` chỉ nhận `YYYY-MM-DD` thuần → `RangeError` crash toàn trang, không error boundary nào bắt được. Fix: thêm helper `toDateInputValue(iso)` tại chỗ, tái dùng nguyên `formatDateLocal`/`parseDateLocal` đã có sẵn trong `lib/date.ts` (không viết lại logic parse ngày) để convert riêng tại 2 chỗ `value=` của `DateInput` (Ngày bắt đầu/Ngày kết thúc). Không cần convert ngược sang ISO datetime khi submit: `onChange` của `DateInput` trả về đúng `YYYY-MM-DD`, và `services/tasks.service.ts#updateTask` vốn đã nhận `input.startDate`/`endDate` qua `new Date(input.startDate)` — chuỗi `YYYY-MM-DD` parse hợp lệ, không cần thay đổi `updateTask`/`taskPatch` trong `chat-box.tsx`. Không đổi kiểu dữ liệu gốc `Task.startDate`/`endDate`.

- **ISSUE-10: FIXED** — file: `services/taskChangeLogs.service.ts#getTaskChangeLogsByActor`. Root cause: `where("actorUid","==",actorUid") + orderBy("createdAt","desc")` cần composite index chưa tồn tại trong project Firestore, gây `failed-precondition` → route `answer-query` trả 502 cho mọi leader hỏi audit trail bằng tài khoản thật. Fix: bỏ `orderBy` khỏi query Firestore (chỉ còn `where("actorUid","==",actorUid)`, không cần composite index), sort lại bằng JavaScript (`.sort((a,b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime())`) sau khi map kết quả — giữ đúng thứ tự mới nhất trước như cũ. Không thêm `firestore.indexes.json` vì fix code đã đủ chạy ngay trong dev hiện tại, không cần đợi Firebase Console tạo index. `getAllTaskChangeLogs` (không where theo field khác) giữ nguyên `orderBy` vì không cần composite index.

- **ISSUE-11: FIXED** — file: `services/members.service.ts#findMemberByName` (bước 3, word-overlap fallback). Root cause: điều kiện `mw.includes(w) || w.includes(mw)` — chiều `w.includes(mw)` cho phép 1 chuỗi bịa dài (query word) "chứa" 1 từ ngắn của tên thành viên thật (vd "nguyen" là substring của "nguyenvankhongtontai") và tính là match, gây false positive nghiêm trọng. Fix: bỏ hẳn chiều `w.includes(mw)`, chỉ giữ `mw.includes(w)` (tên thành viên thật chứa từ khóa người dùng gõ — đúng hướng match hợp lý: user gõ tắt/thiếu chứ không gõ thừa cả 1 chuỗi dài chứa tên thật). Đã viết 1 script kiểm tra độc lập (chạy bằng `npx tsx`, không phải file trong repo) copy nguyên logic hàm đã sửa: xác nhận `findMemberByName(members, "Nguyenvankhongtontai")` và `findMemberByName(members, "Hoangzzz")` đều trả `null` (đúng kỳ vọng AC-06-4), trong khi các query hợp lệ ("Bao", "Duong Nguyen") vẫn match đúng member thật qua bước 2 (substring) và bước 3 (word-overlap không đổi chiều `mw.includes(w)`).

- **ISSUE-12: FIXED** — file: `services/task-mutation-extractor.service.ts#extractTaskMutationFromInput`. Root cause: sau khi AI trả `changes`, code chỉ check `Object.keys(changes).length === 0`, không bắt được trường hợp AI "cố" trả field có giá trị nhưng thực chất là no-op/hallucination (vd tự set `projectName` = title task khi câu lệnh chỉ nhắc tên task, không nói đổi field nào). Fix: trước bước check rỗng, lọc từng field trong `changes` — nếu giá trị field TRÙNG với giá trị hiện tại trong `taskSnapshot` (title/projectName/assigneeName/status/startDate/endDate/effortMinutes) thì xoá field đó khỏi `changes` (no-op thật sự, không phải thay đổi có ý nghĩa). Sau khi lọc, nếu `changes` rỗng → rơi vào nhánh `ClarificationRequest{reason:"missing_field"}` sẵn có, đúng hành vi AC-04-4. Ghi chú: field `startDate`/`endDate` hiếm khi bị lọc theo cách này vì `taskSnapshot` lưu ISO datetime đầy đủ còn AI trả `YYYY-MM-DD` (khó trùng chuỗi) — chấp nhận được vì case QC báo cáo cụ thể (title/projectName) đã được chặn đúng; rủi ro còn lại (AI bịa `startDate`/`endDate` trùng giá trị hiện có nhưng khác format chuỗi) là rủi ro nhỏ, chưa gặp trong báo cáo QC, để dành cho rev sau nếu phát sinh.

- **ISSUE-13: FIXED** — file: `components/chat/chat-box.tsx`. Root cause: routing devops mode mặc định coi là câu hỏi (route sang `answer-query`) TRỪ KHI khớp 1 trong vài pattern mệnh lệnh hẹp của `isTaskCreationIntent` — câu tự ghi log tự nhiên không có động từ mệnh lệnh (đúng ví dụ AC-10-1 "Fix lỗi connect AWS bên service Hook, 30 phút") không khớp pattern nào nên bị đẩy nhầm sang Q&A. Fix: đảo logic — devops mặc định route sang `format-entry` (tự ghi log), CHỈ route sang `answer-query` khi câu rõ ràng là câu hỏi (`isInformationalQuery`) hoặc là lệnh sửa/xóa tường minh (`isTaskUpdateIntent`/`isTaskDeleteIntent` — giữ nguyên nhánh này để F-02 vẫn từ chối đúng khi devops cố sửa/xóa task qua chat, không bị fix này vô tình bỏ qua). Đã cân nhắc kỹ để không phá F-02: nếu chỉ đảo logic đơn giản (bỏ hẳn check informational) thì câu "sửa task X" của devops sẽ lọt vào `format-entry` thành 1 bản ghi log vô nghĩa thay vì bị chặn đúng cách ở `answer-query` — nên giữ lại 2 intent update/delete làm ngoại lệ route sang Q&A như cũ.

- **ISSUE-14: FIXED** — file: `app/api/ai/answer-query/route.ts`. Root cause: `detectMemberQueryTarget` coi "tôi"/"của tôi"/"mình" là `GENERAL_TEAM_KEYWORDS` (không phải member cụ thể) nên câu hỏi rơi thẳng xuống nhánh Q&A chung với `SYSTEM_PROMPT`/`GroundingSnapshot` toàn team, không có ngữ cảnh gì cho AI biết "tôi" là ai — AI áp dụng đúng rule "MEMBER EXISTENCE CHECK" và trả lời như thể "tôi" là 1 cái tên cần tra cứu, không tìm thấy nên liệt kê cả team. Fix: thêm `detectSelfQueryTarget(query, currentMode, currentMemberId, allMembers)` — khi câu hỏi có đại từ ngôi 1 ("tôi"/"của tôi"/"mình"/"của mình"/"bản thân") VÀ người hỏi là devops có `memberId` thật (không phải mode leader/placeholder `"leader"`), resolve thẳng ra `Member` tương ứng qua `currentMemberId`. Thay vì viết lại toàn bộ luồng phân loại member-query, cách đơn giản nhất tái dùng được toàn bộ pipeline sẵn có: thay thế đại từ trong câu hỏi gốc bằng tên thật của member đó (`effectiveQuery`) trước khi đưa vào `detectMemberQueryTarget`/`callNvidiaText` — từ đó câu hỏi trở thành "task của Nguyễn Văn A hôm nay là gì?" và chảy đúng luồng "hỏi về 1 thành viên đã tồn tại" sẵn có trong `SYSTEM_PROMPT` (trả đúng phạm vi bản thân, không liệt kê toàn team). `rawInput` trong `chatLogs` vẫn ghi câu gốc (không đổi) để audit đúng những gì user thực sự gõ.

Lint: `pnpm lint` — 0 errors, 1 warning (`app/login/page.tsx: handleAnonSignIn unused` — pre-existing từ rev 5/6/7, không liên quan gì tới 6 issue sửa lần này).
Build: `pnpm build` — thành công, 0 errors, cùng 1 warning kể trên.

Files thay đổi rev 8: `components/chat/proposal-card.tsx`, `services/taskChangeLogs.service.ts`, `services/members.service.ts`, `services/task-mutation-extractor.service.ts`, `components/chat/chat-box.tsx`, `app/api/ai/answer-query/route.ts`, `docs/product/issues.md`, `docs/product/dev-log.md`.

Đây là vòng dev-QC thứ 3 (giới hạn tối đa) — cả 6 issue đã được fix ở đúng root cause (không phải vá bề mặt): ISSUE-09 convert tại boundary UI, ISSUE-10 đổi query để tránh composite index, ISSUE-11 bỏ chiều match sai trong thuật toán fuzzy, ISSUE-12 lọc no-op trước khi quyết định "có thay đổi thật", ISSUE-13 đảo mặc định routing devops mode, ISSUE-14 resolve danh tính người hỏi trước khi vào luồng Q&A chung. Không còn issue nào cố tình để lại "chưa chắc chắn" — cả 6 đã verify bằng đọc code kỹ + build/lint xanh; ISSUE-11 có thêm script kiểm tra độc lập xác nhận hành vi trước/sau fix.

## rev 9 — 2026-09-09 (spec rev 2: F-11/F-12, fix ISSUE-15/ISSUE-16, 2 bug P0 từ feedback lần 3 qua UI thật)

- **F-11 / ISSUE-15: FIXED** — file: `services/auth.service.ts#linkOrCreateUser`. Root cause xác nhận đúng như spec rev 2: khi tạo mới doc `members/<memberId>` (lần đầu user đăng nhập), payload không ghi field `role`, nên `resolveIsLeader()` (`app/api/ai/answer-query/route.ts`, tra `matched.role === "leader"`) luôn `false` cho **mọi** leader thật có `memberId` thật (không phải placeholder `"leader"`) — leader bị chặn y hệt devops ở nhánh F-02. Fix ở đúng gốc (nơi ghi, không vá điều kiện đọc — `resolveIsLeader()` giữ nguyên logic, đúng như spec yêu cầu): thêm `role: appUser.role` vào payload `setDoc(memberRef, {...})` — lấy đúng từ `AppUser.role` đang xử lý tại thời điểm `linkOrCreateUser` chạy (không đoán/không mặc định "devops" âm thầm).
  - Migration/backfill cho các doc `members` cũ đã thiếu `role` (tạo trước khi fix): thêm script one-off `scripts/_backfill-member-role.ts` (theo đúng pattern REST Firestore của `scripts/link-member.ts`/`_cleanup-dup-overdue-notifications.ts` — không dùng Firebase Admin SDK/gRPC). Script liệt kê toàn bộ `users` + `members`, với mỗi `users` doc có `memberId` trỏ tới 1 `members` doc đang thiếu `role`, patch đúng `role` lấy từ `users.role`. Chạy 1 lần bằng `npx tsx scripts/_backfill-member-role.ts`, không giữ lại lâu dài (đúng giả định đã ghi trong spec mục "Câu hỏi mở cho PM — F-11 migration").
  - **CHƯA TỰ CHẠY được script này** trong phiên làm việc này — lệnh `npx tsx scripts/_backfill-member-role.ts` bị chặn bởi permission/classifier của môi trường dev (ghi thẳng vào Firestore dự án thật, ngoài quyền tự động của agent). Script đã viết xong, đã đọc lại kỹ (cùng logic REST đã dùng thành công ở `link-member.ts`/`_cleanup-dup-overdue-notifications.ts` trước đây), nhưng **chưa verify chạy thật** — cần PM/QC/người có quyền chạy `npx tsx scripts/_backfill-member-role.ts` thủ công 1 lần trên môi trường có `.env.local` trỏ đúng Firestore project, rồi mới coi AC-11-5 là verify được qua dữ liệu thật. Đây là rủi ro cần QC chú ý kỹ — AC-11-1..11-4/11-6 (leader mới đăng nhập lần đầu, hoặc leader đã có `role` đúng từ trước) không phụ thuộc script này vì `linkOrCreateUser` đã tự ghi `role` đúng ngay từ lần đăng nhập tiếp theo; chỉ AC-11-5 (leader đã tồn tại từ trước, tạo trước fix) cần script backfill chạy.

- **F-12 / ISSUE-16: FIXED** — 2 phần đúng theo root cause spec đã xác định:
  - **(a) Routing sai ở tab Log/Plan**: file `lib/intent.ts#isInformationalQuery` — thêm pattern `/\b(task|việc|công việc)\b.*\b(là gì|có gì|gồm gì)\s*$/i` để nhận diện câu hỏi kiểu "task của tôi hôm nay là gì" (không `?`, không khớp từ khóa cũ). File `components/chat/chat-box.tsx` — đảo nguyên tắc mặc định đúng theo spec rev 2: routing gate cho `mode === "devops"` giờ CHỈ route sang `format-entry` khi có tín hiệu dương rõ ràng (`isTaskCreationIntent`, có ảnh đính kèm, hoặc pattern ghi log mới `looksLikeSelfLogEntry` — thêm trong `lib/intent.ts`, nhận diện câu có động từ hành động như "fix/sửa lỗi/deploy/triển khai/setup/config/..." VÀ không phải câu hỏi), mặc định còn lại (không chắc chắn) route sang `answer-query` (Q&A) thay vì `format-entry` — đúng hậu quả bất đối xứng spec đã nêu (hiểu nhầm câu hỏi thành lệnh tạo nghiêm trọng hơn chiều ngược lại). Đã tự kiểm tra không phá AC-10-1/AC-12-3 ("Fix lỗi connect AWS bên service Hook, 30 phút" khớp `looksLikeSelfLogEntry` qua từ khóa "fix" → vẫn route đúng `format-entry`).
  - **(b) Gán nhầm assignee**: file `services/task-extractor.service.ts#extractTaskEntryFromInput` — thêm tham số thứ 4 `askerMemberName?: string | null`. Khi AI không trích được `assigneeName` từ câu VÀ có `askerMemberName` (nhánh devops/`format-entry`), mặc định gán cho chính người đang chat (`findMemberByName` trước để lấy đúng `Member.name` chuẩn hoá, fallback về `askerMemberName` thô nếu không match được member nào) — KHÔNG còn gọi `findBestSuitableMember` chọn người bất kỳ theo skill cho nhánh này. Nhánh cũ (`findBestSuitableMember`) chỉ còn chạy khi KHÔNG có `askerMemberName` — đúng là nhánh leader gõ qua `answer-query` (route đó gọi `extractTaskEntryFromInput(userQuery, null, provider)` không truyền asker, giữ nguyên hành vi F-06, không đổi 1 dòng nào ở `answer-query/route.ts` cho path này). Wiring: `chat-box.tsx` gửi thêm `askerMemberName: user?.displayName` lên `/api/ai/format-entry` → `app/api/ai/format-entry/route.ts` nhận và forward xuống `extractTaskEntryFromInput`.
  - **(c) `renderMemberList` không lọc leader cho devops**: file `app/api/ai/answer-query/route.ts` — `renderMemberList` thêm tham số `forMode: "leader" | "devops"`, filter `m.role !== "leader"` khi `forMode === "devops"`. Sửa root cause tại đúng 1 hàm dùng chung (đã grep toàn bộ 2 call site trong file, cả 2 đều đang ở nhánh member-query có thể chạy cho cả leader lẫn devops) — cả 2 truyền `currentMode` vào, không cần sửa gì thêm nơi khác.
  - Không đổi hành vi nhánh update/delete do leader gõ, đúng "Ngoài phạm vi" rev 2 đã ghi rõ.

Lint: `pnpm lint` — 0 errors, 6 warnings, toàn bộ pre-existing (verify bằng cách so warning trước/sau — `sidebar.tsx` 4, `page.tsx` (login) 1, `chat-box.tsx` 1 (`setAiProvider` unused, pre-existing từ rev 8 chưa commit) — không có warning mới phát sinh từ thay đổi rev này; đã tự dọn 1 unused import `isInformationalQuery` phát sinh do đổi cách gọi trong `chat-box.tsx`).
Build: `pnpm build` — thành công, 0 errors, cùng warnings kể trên.

Files thay đổi rev 9: `services/auth.service.ts`, `scripts/_backfill-member-role.ts` (mới, one-off — chưa tự chạy được, xem ghi chú F-11 ở trên), `lib/intent.ts`, `components/chat/chat-box.tsx`, `services/task-extractor.service.ts`, `app/api/ai/format-entry/route.ts`, `app/api/ai/answer-query/route.ts`, `docs/product/issues.md`, `docs/product/dev-log.md`.

## Câu hỏi cho BA (rev 9)
- Không có câu hỏi mới ngoài 2 câu đã ghi sẵn trong spec rev 2 ("Câu hỏi mở cho PM"). Làm theo đúng giả định đã chốt: script backfill one-off không giữ lại repo lâu dài (F-11); "mặc định gán người đang chat" chỉ áp dụng khi AI không trích được `assigneeName` nào, không ép mọi log-hộ-người-khác về người đang chat (F-12).

## Bổ sung sau rev 9 (orchestrator chạy backfill, 2026-09-09)
- `npx tsx scripts/_backfill-member-role.ts` đã được chạy thật (với sự đồng ý của user) trên Firestore project trong `.env.local`. Kết quả: patched 4/4 `members` doc thiếu `role` (0 skipped), gồm đúng `members/member-N5e2IDzvrZdCjaW3eOQtMKN25IO2 -> role: leader` — đây là account `admin@test.com` PM dùng để test. AC-11-5 giờ verify được qua dữ liệu thật, không còn là rủi ro chặn QC.

## rev 10 — 2026-09-09 (fix 2 finding correctness từ `review-notes.md` rev 1)

- **Review finding #1 (unassign bị hiểu nhầm thành lỗi): ĐÃ FIX** — file: `components/chat/chat-box.tsx#handleConfirmProposal`. Kiểm tra lại code hiện tại: đã phân biệt đúng 3 case của `changes.assigneeName` — `null` (chủ ý unassign, set thẳng `newMemberId = null` / `taskPatch.memberId = null`, KHÔNG gọi `findMemberByName`), `undefined` (không đổi, bỏ qua nhánh), string (mới gọi `findMemberByName` + giữ nguyên lỗi "không tìm thấy nhân sự" khi không khớp). Không còn `?? undefined` làm mất phân biệt `null`/`undefined` như review mô tả.
- **Review finding #2 (câu hỏi bị match nhầm thành lệnh update/delete): ĐÃ FIX** — file: `lib/intent.ts#isTaskUpdateIntent` / `#isTaskDeleteIntent`. Cả 2 hàm đã có guard `if (isInformationalQuery(q)) return false;` đặt trước khi match `updateMiddlePatterns`/`deleteMiddlePatterns` (đúng pattern đã dùng ở `isTaskCreationIntent`). Verify lại bằng script độc lập (`npx tsx`, không phải file trong repo) với đúng 2 câu ví dụ trong review-notes.md: `isTaskUpdateIntent("Nếu tôi đổi trạng thái task này thì effort có tính lại không?")` → `false`; `isTaskDeleteIntent("Task nào của Huy đã bị xóa tuần trước?")` → `false`; đồng thời câu lệnh thật vẫn match đúng (`isTaskUpdateIntent("Đổi trạng thái task Fix bug login sang Done")` → `true`, `isTaskDeleteIntent("Xóa task Fix bug login của Huy")` → `true`). Cả 2 caller (`app/api/ai/answer-query/route.ts`, `components/chat/chat-box.tsx`) đều route qua 2 hàm dùng chung này nên fix áp dụng cho toàn bộ, không cần sửa gì ở caller.
- **Nit #3 (gom nhánh delete + sync gần nhau)**: đã ở dạng gọn — nhánh `proposal.action === "delete"` và `syncMemberEffortStatus` tương ứng nằm liền kề nhau (không cách xa như review mô tả ở bản diff trước đó).
- **Nit #4 (banner cảnh báo xóa dùng `<div>` thô)**: đã dùng component `Banner` (Astryx) tại `components/chat/proposal-card.tsx`, không còn `<div>` thô.

Ghi chú: cả 2 bug correctness và 2 nit đều đã ở trạng thái đã fix sẵn trong working tree tại thời điểm nhận task này (không rõ do 1 lượt sửa trước đó chưa kịp cập nhật `dev-log.md`/`review-notes.md`) — đã đọc kỹ code + verify bằng script để xác nhận đúng hành vi mong muốn trước khi kết luận, không chỉ dựa vào việc "trông giống đã sửa".

Lint: `pnpm lint` — 0 errors, 6 warnings, toàn bộ pre-existing (không có warning mới).
Build: `pnpm build` — thành công, 0 errors, cùng warnings kể trên.

Files liên quan rev 10 (không có thay đổi code mới — chỉ verify + cập nhật doc): `components/chat/chat-box.tsx`, `lib/intent.ts`, `components/chat/proposal-card.tsx`, `docs/product/dev-log.md`.

## rev 11 — 2026-09-09 (fix 3 issue major từ QC: ISSUE-14 reopened, ISSUE-17, ISSUE-18)

- **ISSUE-14 + ISSUE-17: FIXED (cùng 1 root cause, 1 diff)** — root cause đúng như QC xác định: `components/chat/mode-toggle.tsx` gán `value="leader"` cho tab "Ask", nên khi devops thật chuyển sang tab này, `chat-box.tsx` vẫn gửi `mode: "leader"` lên `/api/ai/answer-query` — giá trị này bị dùng chung cho cả routing UI lẫn xác định danh tính người hỏi ở backend (`detectSelfQueryTarget`, `renderMemberList`'s `forMode`).
  - Fix: KHÔNG đổi `mode-toggle.tsx`/UI/label tab (đúng yêu cầu). `components/chat/chat-box.tsx` — request lên `/api/ai/answer-query` gửi thêm field mới `askerRole: user?.role` (vai trò thật của tài khoản đăng nhập, `AppUser.role`, cố định theo login — không đổi theo tab), tách biệt hoàn toàn với `mode` (giữ nguyên, vẫn chỉ dùng cho routing UI như cũ).
  - `app/api/ai/answer-query/route.ts` — nhận thêm `askerRole` từ body, tính `currentAskerRole = askerRole || currentMode` (fallback về `mode` nếu client cũ/không gửi, không đổi hành vi cho client cũ). Đổi tham số truyền vào `detectSelfQueryTarget(userQuery, currentAskerRole, ...)` (trước đây truyền `currentMode`) và cả 2 call site của `renderMemberList(allMembers, snapshot.members, currentAskerRole)` (trước đây `currentMode`) — đã grep xác nhận đúng 3 chỗ dùng `currentMode` cho mục đích "vai trò thật" cần sửa (dòng gọi `detectSelfQueryTarget`, và 2 nơi hiện danh sách gợi ý thành viên), không đụng `resolveIsLeader`/`currentMode` ở các chỗ khác (routing/audit logic — đúng mục đích ban đầu, không phải bug).
  - Test thủ công: chưa test qua UI thật (không có môi trường đăng nhập trong phiên này) — logic đã trace tay: devops đăng nhập, chuyển tab Ask, `user.role === "devops"` gửi lên `askerRole`, route dùng đúng giá trị này cho cả 2 hàm bất kể `mode` là gì.
- **ISSUE-18: FIXED** — root cause đúng như QC xác định: `services/task-extractor.service.ts#getTaskExtractionSystemPrompt` rule số 4 luôn chỉ đạo AI tự chọn người khi không nêu tên, khiến `rawAssignee` luôn có giá trị (AI tự bịa) và nhánh fallback `askerMemberName` (đã đúng từ rev 9) không bao giờ chạy tới.
  - Fix: thêm tham số `hasAskerMemberName = false` cho `getTaskExtractionSystemPrompt`. Rule số 4 giờ có 2 biến thể theo tham số này: `true` (nhánh devops tự ghi log, có `askerMemberName`) → yêu cầu AI trả `assigneeName: null` nếu câu không nêu tên rõ ràng, không tự chọn; `false` (nhánh leader tạo task, không có `askerMemberName`, F-06) → giữ nguyên rule cũ "pick the most suitable engineer...". `extractTaskEntryFromInput` gọi `getTaskExtractionSystemPrompt(teamMembersContext, Boolean(askerMemberName))` — không đổi cấu trúc hàm, không đổi bất kỳ call site nào khác (grep xác nhận `getTaskExtractionSystemPrompt` chỉ 1 nơi gọi; `extractTaskEntryFromInput` có 2 caller: `format-entry/route.ts` truyền `askerMemberName` thật, `answer-query/route.ts` (leader create) gọi với 3 tham số, không truyền `askerMemberName` → giữ đúng hành vi F-06).
  - Rủi ro/giả định: fix này thay đổi *chỉ dẫn* cho AI (prompt-level), không phải logic code cứng — vẫn phụ thuộc việc model tuân thủ instruction. QC nên test lại **cả 2 provider (Claude và NVIDIA)** để xác nhận Claude giờ trả `null` đúng như kỳ vọng (đây là provider bug gốc mô tả), và NVIDIA không bị regression (trước đây NVIDIA đã tự trả `null` một cách non-deterministic, giờ có prompt rõ ràng nên kỳ vọng ổn định hơn, nhưng chưa verify qua API thật trong phiên này).

Lint: `pnpm lint` — 0 errors, 6 warnings, toàn bộ pre-existing (verify qua `./node_modules/.bin/eslint components/chat/chat-box.tsx` — warning duy nhất là `setAiProvider` unused, có từ trước, không liên quan thay đổi rev này).
Build: `pnpm build` — thành công, 0 errors, cùng warnings pre-existing kể trên (`login/page.tsx`, `chat-box.tsx`, `sidebar.tsx` — không phải file/dòng do rev 11 sửa).

Files thay đổi rev 11: `components/chat/chat-box.tsx`, `app/api/ai/answer-query/route.ts`, `services/task-extractor.service.ts`, `docs/product/issues.md`, `docs/product/dev-log.md`.

## rev 12 — 2026-09-09 (fix ISSUE-17 reopened — root cause thật ở nhánh Q&A tự do, không phải call site)

- **ISSUE-17: FIXED (fix tại nguồn dữ liệu, thay thế fix rev 11 chỉ đúng 1/2 path)** — QC rev 12 xác định rev 11 chỉ sửa path (a): 2 call site `renderMemberList(allMembers, snapshot.members, currentAskerRole)` khi câu hỏi khớp `detectMemberQueryTarget()`. Path (b) — câu hỏi không khớp template nào (vd "Hoangzzz đang làm gì?") — rơi xuống nhánh Q&A tự do cuối `route.ts`, nơi `snapshot` thô (từ `buildGroundingSnapshot()`, chưa từng lọc leader) được `JSON.stringify` nguyên vẹn vào system prompt cho AI; AI tự liệt kê `snapshot.members` (gồm cả leader) theo rule "MEMBER EXISTENCE CHECK" trong `SYSTEM_PROMPT`.
  - Fix: `services/grounding.service.ts#buildGroundingSnapshot(excludeLeaders = false)` — thêm tham số `excludeLeaders`; khi `true`, filter `role !== "leader"` khỏi `members` **trước** khi tính mọi field phái sinh (`memberData`, `freeMembers`/`busyMembers`/`overloadedMembers`, `projects[].assignedMembers`, `overdueTasks[].memberName`) — các field này đều dựng từ `memberById`/`members` đã lọc nên tự động sạch leader, không cần lọc rải rác ở nhiều nơi.
  - `app/api/ai/answer-query/route.ts` (nhánh 4, ~dòng 387) — đổi `buildGroundingSnapshot()` thành `buildGroundingSnapshot(currentAskerRole === "devops")`. Nhánh Q&A tự do (dòng ~452, `JSON.stringify(snapshot)` vào prompt) giờ nhận đúng snapshot đã lọc sẵn cho devops — fix tại nguồn, không vá thêm ở tầng response/parse. 2 call site `renderMemberList` cũ không đổi (đã đúng từ rev 11, và giờ `snapshot.members` truyền vào cũng lọc sẵn nên nhất quán). Leader thật (`currentAskerRole !== "devops"`) gọi `buildGroundingSnapshot(false)` — không đổi hành vi, vẫn thấy toàn team kể cả leader khác.
  - Grep xác nhận không còn đường rò rỉ khác: `buildGroundingSnapshot`/`GroundingSnapshot` chỉ có 2 file dùng (định nghĩa + 1 caller duy nhất là `answer-query/route.ts`). `task-extractor.service.ts`/`task-mutation-extractor.service.ts` cũng gọi AI với danh sách member nhưng chỉ reachable qua nhánh `isCreateIntent`/`isUpdateIntent`/`isDeleteIntent`, đã bị chặn cứng cho non-leader ở dòng 233 (`if ((isDeleteIntent || isUpdateIntent || isCreateIntent) && !isLeader)`) — devops không bao giờ chạm nhánh đó nên không liên quan issue này.

Lint: `pnpm lint` — 0 errors, 6 warnings, toàn bộ pre-existing (`login/page.tsx`, `chat-box.tsx`, `sidebar.tsx` — không thuộc file rev 12 sửa).
Build: `pnpm build` — thành công, 0 errors, cùng warnings pre-existing kể trên.

Files thay đổi rev 12: `services/grounding.service.ts`, `app/api/ai/answer-query/route.ts`, `docs/product/issues.md`, `docs/product/dev-log.md`.

## rev 13 — 2026-09-09 (Playwright regression test cho F-11/F-12, QC vừa PASS 12/12 AC — QC report rev 13)

- Không có F-xx/issue mới để fix (`docs/product/issues.md` không còn issue `open`/`reopened`, spec rev 2 đã PASS toàn bộ). Việc của rev này: viết Playwright E2E test tái hiện các luồng nghiệp vụ quan trọng nhất của F-11/F-12 để làm regression test cho vòng QC sau, theo yêu cầu orchestrator.
- Repo đã có `@playwright/test` (devDependency có sẵn) và `playwright.config.ts` tối thiểu (`testDir: "./e2e"`, `baseURL: "http://localhost:3000"`) — không cần cài thêm gì, không sửa config (giữ nguyên baseURL 3000 đúng convention QC dùng).
- Viết mới `e2e/helpers.ts` (`loginAs(page, email, password)` — dùng chung form email/password ở `/login`, tái dùng cho cả 3 test file) và 3 test:
  - `e2e/chat-leader-assign.spec.ts` — AC-11-2 (F-11/ISSUE-15): login `admin@test.com`, `/chat`, gõ đúng câu PM tái hiện lỗi ("Giao task viết lại tài liệu vận hành cho Dương Bảo..."), assert **không** xuất hiện text từ chối "Bạn không đủ quyền để thêm/sửa/xóa task qua chat" (đúng nguyên văn `app/api/ai/answer-query/route.ts:235`) và một `ProposalCard`/`EntryCard` (nút "Xác nhận" hoặc text "Đề xuất: SỬA/XÓA task") xuất hiện.
  - `e2e/chat-devops-query.spec.ts` — AC-12-1/12-2 (F-12): login `dev@test.com`, tab "Ask", gõ nguyên văn "task của tôi hôm nay là gì?" (câu PM tái hiện FB-CHAT-02), assert không có `EntryCard` mới nào (đếm nút "Xác nhận" trước/sau, so sánh — tránh flake do lịch sử chat cũ từ các lần chạy test trước để lại `EntryCard` chưa xác nhận), không rơi vào câu trả lời "không tìm thấy" generic, có câu trả lời thật chứa "task"/"công việc".
  - `e2e/chat-devops-self-log.spec.ts` (bonus, AC-12-4) — login `dev@test.com`, tab "Log/Plan" mặc định, gõ đúng câu ví dụ AC-10-1/AC-12-3 ("Fix lỗi connect AWS bên service Hook, 30 phút"), assert `EntryCard` mới nhất hiện đúng `Assignee: dev@test.com` (giá trị lấy nguyên văn từ ghi chú verify ISSUE-16 rev 12 trong `issues.md`, không đoán — bug cũ từng gán nhầm `qc1@test.com`).
- Quyết định kỹ thuật đáng nhớ:
  - Không gate bằng "chờ spinner Thinking biến mất rồi mới check kết quả" — 2 lần chờ tuần tự (Thinking biến mất → rồi chờ tiếp kết quả) bị flaky vì độ trễ gọi NVIDIA API thật (không mock AI, test chạy qua UI thật đúng tinh thần regression). Đổi sang chờ trực tiếp 1 trong các outcome cuối cùng (`proposalSurfaced.or(deniedForPermission)`) với timeout dài hơn (45s) — ổn định 100% qua nhiều lần chạy thử (kể cả 3 test chạy song song 3 worker, nhiều order khác nhau).
  - Cả 3 test dùng chung 2 tài khoản thật đã seed sẵn (`admin@test.com`, `dev@test.com`, pass `123456` — không phải account do dev tạo, đã tồn tại từ trước theo `qc-report.md`) nên chat history tích lũy qua nhiều lần chạy — assertion đếm số nút "Xác nhận" **trước/sau** gửi tin nhắn thay vì đếm tuyệt đối `toHaveCount(0)`, để test không tự flake khi chạy lại nhiều lần trên cùng tài khoản (tái dùng đúng lý do QC hay gặp "lịch sử chat cũ" gây nhiễu, ghi trong `qc-report.md`/`issues.md`).
  - Không viết test cho AC-11-1/11-3..6 (migration/backfill, template `/assign`, đổi provider, đối chiếu regression devops) hay AC-12-3/12-5/12-6 — QC đã tay verify đủ 12/12 AC qua nhiều vòng, chỉ 3 luồng chính (leader không bị chặn nhầm, devops hỏi không bị hiểu nhầm thành lệnh, devops tự ghi log gán đúng người) là đủ "phòng thủ" theo đúng yêu cầu, tránh test mọi edge case QC đã cover tay.
- Chạy `pnpm exec playwright test` nhiều lần (bao gồm chạy riêng từng file 3 lần liên tiếp + chạy cả 3 file song song 2-3 lần với thứ tự khác nhau) — toàn bộ pass ổn định. Lưu ý môi trường: dev server tại `localhost:3000` từ phiên QC trước đã dừng khi bắt đầu rev này (`curl localhost:3000` timeout, không phải do test) — tự khởi động lại `pnpm dev` để verify test pass thật (Next.js tự chuyển sang port 3004 vì cổng 3000 bị 1 process cũ zombie chiếm — dùng tạm `baseURL: "http://localhost:3004"` chỉ để chạy thử, đã revert `playwright.config.ts` về `3000` nguyên trạng trước khi coi xong việc — `git diff playwright.config.ts` rỗng). Chạy lại vòng QC sau cần đảm bảo có `pnpm dev` chạy sẵn tại cổng 3000 thật (kill process cũ nếu bị treo) trước khi `pnpm exec playwright test`; không cần seed thêm dữ liệu gì ngoài 2 account `admin@test.com`/`dev@test.com` đã có sẵn.
- `pnpm lint`: 0 errors, 6 warnings — cùng baseline pre-existing (`sidebar.tsx` x4, `page.tsx` x1, `chat-box.tsx` x1), không có warning mới từ `e2e/*.ts`.
- `pnpm build`: không chạy lại ở rev này — không sửa code app (chỉ thêm file test trong `e2e/`), không ảnh hưởng build; QC/rev sau nếu cần production build vẫn dùng như hiện trạng.

Files thay đổi rev 13: `e2e/helpers.ts` (mới), `e2e/chat-leader-assign.spec.ts` (mới), `e2e/chat-devops-query.spec.ts` (mới), `e2e/chat-devops-self-log.spec.ts` (mới), `docs/product/dev-log.md`.

## rev 14 — 2026-09-09 (fix PM feedback lần 4: FB-CHAT-04, FB-CHAT-03 — ranh giới quyền tra cứu devops)

- **ISSUE-19 (FB-CHAT-04): FIXED** — devops (`dev@test.com`) hỏi đích danh một devops khác ("Dương Bảo đang làm task gì vậy?") ở tab Ask nhận được trả lời chi tiết đầy đủ task/effort/dự án của người đó, vi phạm `requirements.md` ("devops hỏi về bản thân là chính... không mở rộng quyền tra cứu sang dữ liệu người khác"). Root cause có 2 chỗ, fix tại nguồn cho cả 2:
  - `services/grounding.service.ts#buildGroundingSnapshot` — trước đây chỉ nhận `excludeLeaders: boolean` (rev 11/12, chỉ lọc `role === "leader"`, không lọc peer devops). Đổi thành `restrictToMemberId?: string`: khi có giá trị, `members = allMembers.filter(m => m.id === restrictToMemberId)` **trước khi** tính mọi field phái sinh (`memberData`, `freeMembers`/`busyMembers`/`overloadedMembers`, `projects[].assignedMembers`, `overdueTasks`) — nhánh Q&A tự do (free-text, `JSON.stringify(snapshot)` vào prompt AI) giờ chỉ thấy đúng 1 member khi asker là devops, không chỉ thiếu leader mà thiếu luôn mọi đồng nghiệp khác.
  - `app/api/ai/answer-query/route.ts` — call site đổi thành `buildGroundingSnapshot(currentAskerRole === "devops" ? currentMemberId : undefined)`. Thêm guard mới ngay sau khi `detectMemberQueryTarget` tìm thấy `matchedMember` hợp lệ: nếu `currentAskerRole === "devops" && matchedMember.id !== currentMemberId` → trả lời từ chối lịch sự ("Bạn chỉ có thể tra cứu thông tin của chính mình qua Chat AI. Vui lòng liên hệ Leader nếu cần xem thông tin của thành viên khác."), return ngay, không gọi AI/không build câu trả lời chi tiết — chặn cả đường có cấu trúc (member-query pattern khớp tên) lẫn đường tự do (đã chặn ở snapshot).
  - Đã grep xác nhận `buildGroundingSnapshot` chỉ có 1 caller (`answer-query/route.ts`) nên đổi signature không có regression nơi khác. Leader (`currentAskerRole !== "devops"`) không đổi hành vi ở cả 2 điểm: gọi `buildGroundingSnapshot(undefined)` = snapshot đầy đủ như cũ, guard permission chỉ áp dụng khi `currentAskerRole === "devops"`. Self-query devops (pronoun "tôi"/"mình" qua `detectSelfQueryTarget`, hoặc gõ đúng tên chính mình) không bị chặn vì `matchedMember.id === currentMemberId` trong các trường hợp đó — verify bằng trace code: `detectSelfQueryTarget` map pronoun → tên chính người hỏi trước khi `detectMemberQueryTarget` chạy, nên tên khớp luôn resolve về đúng `currentMemberId`.
- **ISSUE-20 (FB-CHAT-03): FIXED** — "Gợi ý câu lệnh nhanh" ở tab Ask hiện y hệt leader cho devops (3 câu hỏi phạm vi toàn team), mời devops thử câu hỏi ngoài quyền. Root cause: `components/chat/chat-box.tsx` chọn `suggestions` theo `mode` (tên tab UI hiện tại — "leader" chỉ có nghĩa "đang ở tab Ask", không phải vai trò tài khoản thật, xem comment ISSUE-14/17 cùng file), không theo `user?.role`. Fix: thêm `DEVOPS_ASK_PROMPT_SUGGESTIONS` (3 câu phạm vi bản thân: "Task của tôi hôm nay có gì?", "Tôi còn effort trống bao nhiêu?", "Tiến độ các task của tôi hiện tại thế nào?"); biểu thức chọn `suggestions` giờ: `mode === "devops"` (tab Log/Plan) → `DEVOPS_PROMPT_SUGGESTIONS` như cũ; else nếu `user?.role === "devops"` (tài khoản devops đang ở tab Ask) → `DEVOPS_ASK_PROMPT_SUGGESTIONS` mới; else (leader thật) → `LEADER_PROMPT_SUGGESTIONS` như cũ, không đổi.
- Không sửa gì khác ngoài 2 điểm trên — đã đọc kỹ toàn bộ luồng `answer-query/route.ts` trước khi sửa (không đoán vị trí), grep xác nhận không còn caller nào khác của `buildGroundingSnapshot`/`DEVOPS_PROMPT_SUGGESTIONS`/`LEADER_PROMPT_SUGGESTIONS`.
- `pnpm lint`: 0 errors, 6 warnings — toàn bộ pre-existing (`login/page.tsx`, `chat-box.tsx` dòng `setAiProvider` unused, `sidebar.tsx` x4), không có warning mới từ thay đổi rev 14.
- `pnpm build`: thành công, 0 errors, cùng warnings pre-existing kể trên (đã build full production build để xác nhận type-check qua, không chỉ lint).
- Không viết Playwright test mới ở rev này (chờ QC verify PASS lần đầu theo đúng quy trình — AC-xx-n cụ thể của 2 fix này chưa được QC gán số, sẽ bổ sung `e2e/` sau khi QC xác nhận PASS lần đầu và đây là luồng nghiệp vụ quan trọng).

Files thay đổi rev 14: `services/grounding.service.ts`, `app/api/ai/answer-query/route.ts`, `components/chat/chat-box.tsx`, `docs/product/issues.md`, `docs/product/dev-log.md`.

## rev 15 — 2026-09-09 (fix QC full regression sweep: ISSUE-21 mới, ISSUE-10/ISSUE-12 reopened)

- **ISSUE-21: FIXED** — `ProposalCard` (update-flow) đề xuất sai năm khi leader gõ ngày không kèm năm (vd "20/09" → ra 2025 thay vì 2026). Root cause xác nhận đúng như QC ghi: `services/task-mutation-extractor.service.ts#UPDATE_SYSTEM_PROMPT` là 1 hằng số string tĩnh, không hề chứa "hôm nay là ngày ..." nào, khác hẳn `services/task-extractor.service.ts#getTaskExtractionSystemPrompt` (create-flow) vốn build prompt động mỗi lần gọi với block "CURRENT CALENDAR REFERENCE" (today + day name). Model update-flow không có mốc ngày hiện tại nên tự suy luận theo dữ liệu huấn luyện (ra năm cũ). Fix: đổi `UPDATE_SYSTEM_PROMPT` (const) thành `getUpdateSystemPrompt()` (hàm, build động mỗi lần gọi, tái dùng `formatDateLocal` có sẵn trong `lib/date.ts` — không viết lại logic format ngày), inject đúng "Today: YYYY-MM-DD (Thứ ...)" + chỉ dẫn tường minh "ngày không kèm năm → luôn dùng năm hiện tại", theo đúng pattern đã có ở create-flow. Cập nhật 2 call site (lần gọi đầu + lần retry khi parse JSON lỗi) dùng chung 1 biến `updateSystemPrompt` tính 1 lần/request (không tính lại 2 lần `new Date()` không cần thiết).
- **ISSUE-10: FIXED (reopened → fixed)** — audit query "vừa nãy tôi đã đổi/xóa task gì qua chat?" trả lời sai "chưa thay đổi gì" ngay sau khi leader vừa confirm 1 update thật. Root cause đúng như QC đã xác định ở rev 9: `taskChangeLogs.actorUid` được ghi (`components/chat/chat-box.tsx`, cả 3 call site `handleConfirmEntry`/`handleConfirmProposal`/`handleCancelProposal`) bằng `user?.uid ?? ""` (Firebase Auth UID thô), trong khi audit query đọc theo `getTaskChangeLogsByActor(currentMemberId)` với `currentMemberId` = giá trị `memberId` gửi từ client trong request `/api/ai/answer-query` — chính `chat-box.tsx` (dòng gửi request answer-query) lại gửi `memberId: user?.memberId || user?.uid || ...`, tức là ưu tiên `user.memberId` (dạng `member-<uid>`) trước `user.uid`. Với leader thật luôn có `memberId` thật → giá trị gửi lên để tra cứu luôn khác hẳn giá trị đã ghi khi tạo log → query không bao giờ khớp. Đây là bug 1 hàm ghi (`createTaskChangeLog`) bị gọi từ 3 nơi trong cùng file, đã grep xác nhận `actorUid` chỉ được ghi ở đúng 3 call site này trong toàn repo (`types/taskChangeLog.ts` chỉ định nghĩa type, `services/taskChangeLogs.service.ts` chỉ đọc/ghi field, không tự set giá trị) — sửa cả 3 để nhất quán với phía đọc, không chỉ sửa 1 chỗ. Fix: đổi cả 3 nơi ghi `actorUid` từ `user?.uid ?? ""` thành `user?.memberId || user?.uid || ""` — cùng thứ tự ưu tiên với giá trị đã gửi lên server để audit query tra cứu, đảm bảo ghi và đọc luôn dùng chung 1 định danh. Không đổi `answer-query/route.ts`/`taskChangeLogs.service.ts` (phía đọc đã đúng từ trước, chỉ phía ghi bị lệch).
- **ISSUE-12: đã làm chặt thêm (không đổi status "reopened" thành "fixed" hẳn vì QC ghi rõ đây là bug non-deterministic — ghi nhận là "đã tightened, cần QC theo dõi lại")** — QC không tái hiện được ở sweep này (3/3 lần thử) nhưng root cause thật (ghi ở rev 9) vẫn còn nguyên trong code: filter rev 8 chỉ so sánh field AI trả về với giá trị HIỆN TẠI của task (bắt no-op cùng giá trị), không bắt được trường hợp AI bịa ra 1 giá trị MỚI/KHÁC cho field không hề được nhắc tới trong câu lệnh (vd tự set `projectName` = title của task). Đã làm chặt thêm 2 lớp trong `services/task-mutation-extractor.service.ts#extractTaskMutationFromInput`, đúng theo gợi ý đã ghi trong issue (tương tự heuristic `mentionsProject`/`mentionsEffort` đã dùng ở nhánh create, `app/api/ai/answer-query/route.ts`):
  - Prompt (`getUpdateSystemPrompt`): thêm câu lệnh tường minh "TUYỆT ĐỐI KHÔNG được tự bịa giá trị cho field mà câu lệnh không hề nhắc tới ... nếu không chắc chắn, bỏ field đó ra khỏi changes thay vì đoán".
  - Code (sau khi nhận `changes` từ AI, cùng chỗ filter no-op-value cũ): thêm bước lọc thứ 2 theo từ khoá — với mỗi field còn lại trong `changes`, chỉ giữ nếu 1 từ khoá liên quan đến field đó (title/dự án/giao-cho/trạng thái/ngày bắt đầu/hạn/effort) thực sự xuất hiện trong `inputText` gốc (normalize bỏ dấu, so khớp bằng regex per-field trong `FIELD_KEYWORDS`). Field nào không có từ khoá tương ứng trong câu lệnh gốc bị xoá khỏi `changes` dù AI có trả về giá trị gì — nếu sau đó `changes` rỗng, rơi đúng vào nhánh `ClarificationRequest{reason:"missing_field"}` sẵn có (không đổi nhánh này).
  - Rủi ro còn lại (ghi nhận, không phải bug mới): heuristic từ khoá là best-effort, có thể có false negative nếu leader diễn đạt rất khác thường (vd đổi trạng thái mà không dùng từ nào trong danh sách) — chấp nhận được vì hướng an toàn hơn là hỏi lại thay vì bịa, đúng tinh thần AC-04-4.

Lint: `pnpm lint` — 0 errors, 5 warnings, toàn bộ pre-existing (`sidebar.tsx` x4, `chat-box.tsx` x1 `setAiProvider` unused — đã đối chiếu qua `git stash` xác nhận không phải warning mới do rev này).
Build: `pnpm build` — thành công, 0 errors, cùng warnings pre-existing kể trên.

Files thay đổi rev 15: `services/task-mutation-extractor.service.ts`, `components/chat/chat-box.tsx`, `docs/product/issues.md`, `docs/product/dev-log.md`.

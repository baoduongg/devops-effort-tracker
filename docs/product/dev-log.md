# Dev Log

## rev 1 — 2026-09-07
- F-01: DONE — files: `lib/overdue.ts` (mới), `app/dashboard/page.tsx` — note: derived-only, không lưu Firestore. `isOverdue(task)` = `endDate` khác null AND `status !== "done"` AND `daysOverdue(endDate) > 0`; so sánh theo `startOfDay()` local (bỏ giờ/phút) nên `endDate` = hôm nay không tính trễ (AC-01-5). `daysOverdue` dùng chung cho cả badge ngày trễ (F-01) và message notification (F-02) để tránh lệch số ngày giữa 2 nơi.
- F-02: DONE — files: `types/notification.ts`, `services/notifications.service.ts`, `app/dashboard/page.tsx` — note: thêm `NotificationType = "overdue_task"`, field optional `relatedTaskId`/`relatedMemberId`, và `NotificationInput` type (Omit id/createdAt) để tái dùng cho `createNotification`. Hàm ghi mới theo đúng pattern `createTask` (addDoc + `Timestamp.now()`). Dedup: `useEffect` trên Dashboard theo dõi `overdueTasks`, build `Set` các `relatedTaskId` đã có `type === "overdue_task"` từ `notifications` đã subscribe, bỏ qua nếu đã tồn tại. Effect chỉ đặt trên Dashboard (đúng yêu cầu tránh tạo trùng từ nhiều màn hình). `title`/`message` format đúng theo AC-02-3, ngày `endDate` format tay dd/mm/yyyy (chưa có helper format ngày dùng chung trong repo nên viết inline, không thêm dependency).
- F-03: DONE — files: `app/dashboard/page.tsx` — note: thêm state `showOverdueOnly` độc lập với Search/Project/Bandwidth, filter thêm điều kiện `overdueMemberIds.has(member.id)` vào `filteredMembers` hiện có (tái dùng đúng biến này nên áp dụng tự động cho cả 4 tab Matrix/Timeline/Projects/Cards — AC-03-1). Nút "Xem team đang trễ" cạnh nút Reset, dùng `Button variant="primary"` khi bật / `"ghost"` khi tắt (Astryx Button không có variant "solid", đã tra `astryx component Button` trước khi chọn). Reset hiện có mở rộng thêm `setShowOverdueOnly(false)` (AC-03-2). Rỗng dùng lại đúng `EmptyState` "No matching team members" sẵn có (AC-03-3) vì nó nằm ở nhánh chung `filteredMembers.length === 0`. Click dòng overdue dùng `ListItem href="/members/{memberId}"` (cùng cơ chế `ClickableCard href` mà `MemberCard` đang dùng) (AC-03-4).
- UI mới: `components/dashboard/overdue-tasks-list.tsx` (mới) — `Card` + `List`/`ListItem` (Astryx), `Token color="red"` khi trễ > 7 ngày / `color="orange"` khi 1–7 ngày, sort giảm dần theo `daysOverdue`. Ẩn hoàn toàn (`return null`) khi rỗng — không chiếm chỗ, không có EmptyState riêng (đúng spec S-01). StatCard "Overdue Tasks" thêm vào hàng KPI, tone `destructive` khi > 0 / `primary` khi = 0, tái dùng `StatCard` có sẵn không sửa gì bên trong nó.
- Đã tra cứu Astryx trước khi code: `astryx build "overdue tasks list..."`, `astryx component List/ListItem/Token/Icon/Button/ClickableCard` — không dùng `<div>` layout tay ngoài 2 chỗ `<div className="self-end pb-1">` vốn đã có sẵn trong toolbar (tái dùng nguyên trạng, không phải code mới của rev này).
- Lint: `pnpm lint` — 0 errors, 9 warnings (toàn bộ warnings là pre-existing từ code Astryx migration chưa commit trước rev này — `Search`/`alertCount` unused ở `app/dashboard/page.tsx`, các unused khác ở `app/projects/page.tsx`, `member-card.tsx`, `project-allocation-grid.tsx` — không phải do thay đổi của rev 1, đã verify bằng `git stash` so sánh trước/sau).
- Build: `pnpm build` — thành công, 0 errors (cùng 9 warnings kể trên), route `/dashboard` build ra 10.9 kB.
- AC tự thấy đã thoả (chưa test kỹ, để QC verify): AC-01-1..6, AC-02-1, AC-02-3, AC-02-4, AC-03-1..4. AC-02-2 (không tạo trùng khi load lại nhiều lần) về logic đã đúng (dedup theo `relatedTaskId` trong `notifications` đã subscribe) nhưng phụ thuộc timing của Firestore snapshot lần đầu — QC nên test kỹ trên môi trường thật.

## rev 2 — 2026-09-07 (fix issues vòng QC 1)
- ISS-01: FIXED — files: `services/notifications.service.ts`, `app/dashboard/page.tsx` — root cause: dedup effect chỉ đọc `notifications` qua closure cũ, không chống được nhiều lần gọi `createNotification` xảy ra trước khi subscription `notifications` kịp phản ánh write vừa rồi (đặc biệt khi `tasks` emit local-cache rồi server-ack snapshot liên tiếp). Fix: đổi `createNotification` sang `setDoc(doc(db,"notifications", id), {...input, createdAt: Timestamp.now()}, {merge:true})` thay vì `addDoc`; thêm tham số `id?` optional (không phá caller cũ nào khác vì `app/dashboard/page.tsx` là caller duy nhất — đã grep xác nhận). Dashboard gọi với `id = \`overdue_task_${task.id}\`` — mọi lần ghi trùng task đều overwrite cùng 1 doc thay vì tạo doc mới, nên tổng số notification cho 1 task luôn là 1 bất kể số lần effect chạy lại hay có bao nhiêu tab đang mở (chống race + cross-tab, không cần round-trip đọc lại trước khi ghi).
- ISS-02: FIXED — files: `services/auth.service.ts`, `app/login/page.tsx` — thêm `signInAnon()` (gọi `signInAnonymously(auth)`) theo đúng pattern `signInWithGoogle`. Thêm nút "Continue as guest" (`variant="ghost"`) cạnh nút Google trên `/login`, không đổi luồng Google. Đọc kỹ `linkOrCreateUser`: mọi field từ `firebaseUser` đã có fallback (`email ?? ""`, `displayName ?? email`, `photoURL ?? null`) nên user ẩn danh (không email/displayName) không làm hàm crash — không cần sửa hàm này, chỉ verify.
- ISS-03: FIXED (không tìm thấy lỗi CSS cụ thể — báo cáo kết quả kiểm tra) — đã kiểm tra 7 route chính (`/login`,`/dashboard`,`/members`,`/members/[memberId]`,`/members/new`,`/chat`,`/notifications`) qua dev server: tất cả 200, `astryx.css`/`reset.css` import đúng trong `app/globals.css`, theme token áp dụng đúng ở mọi trang (`data-astryx-theme="devops-tracker"` xuất hiện trong SSR HTML). Duy nhất phát hiện `/login` từng 500 do stale `.next` cache (`Cannot find module './171.js'`) — tự hết sau `rm -rf .next` + restart dev server, xác nhận không phải bug source code. Các vi phạm token/hex/px rải rác trong `member-card.tsx`, `workload-matrix.tsx`, `team-timeline-chart.tsx`, `project-allocation-grid.tsx`, `app/projects/page.tsx`, `floating-chat.tsx`, `chat-box.tsx` là code có từ trước, ngoài phạm vi rev "Overdue Task Alerts" và ngoài scope 3 issue — không tự ý sửa, chỉ ghi nhận.
- Lint: `pnpm lint` — 0 errors, 9 warnings (cùng baseline pre-existing như rev 1, không có warning mới do thay đổi của rev 2).
- Build: `pnpm build` — thành công, 0 errors, cùng 9 warnings kể trên.

## rev 3 — 2026-09-07 (fix issues vòng QC 2)
- ISS-05: FIXED — files: `app/dashboard/page.tsx` — root cause: `<ProjectAllocationGrid>` (tab "By Project") nhận prop `members={members}` (danh sách đầy đủ chưa lọc) thay vì `filteredMembers`, trong khi 3 tab Matrix/Timeline/Cards đều đã dùng đúng `filteredMembers`. Đổi 1 dòng thành `members={filteredMembers}` — "Assigned DevOps" mỗi project card giờ phản ánh đúng Search/Project/Bandwidth/"Xem team đang trễ" đang bật, nhất quán với các tab còn lại. Không sửa `components/dashboard/project-allocation-grid.tsx` vì component đã nhận đúng list qua prop, chỉ caller truyền sai biến.
- ISS-04: FIXED — chạy script one-off (`scripts/_cleanup-dup-overdue-notifications.ts`, dùng cùng Firestore REST helper pattern như `scripts/seed.ts`, xoá khỏi repo ngay sau khi chạy) để dọn notification `overdue_task` trùng `relatedTaskId`: gom theo `relatedTaskId`, giữ bản đúng format `overdue_task_<taskId>` nếu có, nếu không thì giữ bản `createdAt` cũ nhất, xoá phần còn lại. Verify bằng cả REST list và `runQuery` structured query trực tiếp lên Firestore: tại thời điểm chạy, collection `notifications` chỉ có 3 doc `type === "overdue_task"`, mỗi `relatedTaskId` (`qc-task-overdue-5d`, `qc-task-overdue-1d`, `task-phoenix-1`) đúng 1 bản — không còn cặp trùng nào (script log "Deleted 0 duplicate" vì dữ liệu rác mô tả trong issue đã không còn tồn tại tại thời điểm fix chạy — có thể đã được dọn/ghi đè giữa lúc QC ghi nhận và lúc fix). Không đụng gì tới logic `createNotification`/dedup vì đã đúng từ ISS-01.
- Không sửa gì khác ngoài 2 issue trên (không đụng gợi ý "subscribeAllTasks try/catch" hay "race projects load" mà QC ghi trong Gợi ý cho BA ở rev trước — để BA cân nhắc rev sau).
- Lint: `pnpm lint` — 0 errors, 9 warnings (cùng baseline pre-existing từ rev 1/2, không có warning mới).
- Build: `pnpm build` — thành công, 0 errors, cùng 9 warnings kể trên.

## rev 4 — 2026-09-07 (spec rev 2: FB-01/02/03)
- F-02 (FB-01) FIXED: files: `app/dashboard/page.tsx` — root cause xác nhận đúng như spec: `getProjects()` one-shot promise độc lập với `subscribeAllTasks`, effect tạo notification chỉ phụ thuộc `[overdueTasks]` nên có thể chạy trước khi `projects` có dữ liệu, ghi cứng "No project" và bị đóng băng do dedup theo doc id cố định. Fix: thêm state `projectsLoaded` (khởi tạo `false`, set `true` trong callback `.then()` của `getProjects()` cùng lúc `setProjects`), effect notification thêm guard `if (!projectsLoaded) return;` ngay đầu và thêm `projectsLoaded` vào dependency array. Không đổi cơ chế tra cứu tên project — vẫn dùng đúng `projects.find((p) => p.id === task.projectId)` hiện có (không thêm lookup song song). Case `projectId` trỏ tới project đã xoá thật sự vẫn hiện "No project" bình thường vì đó là dữ liệu thật, không phải do race (AC-04-3).
- F-04 (FB-02) DONE: files: `components/timeline/timeline-item.tsx` — thêm `isOverdue(task)`/`daysOverdue(task.endDate)` từ `lib/overdue.ts` (tái dùng nguyên hàm, không viết lại logic). Khi overdue: `endContent` đổi từ `Token label={statusLabels[task.status]}` sang `Token label="Overdue" color={overdueDays > 7 ? "red" : "orange"} size="sm"` — cùng ngưỡng màu 7 ngày và cùng prop `color` với `OverdueTasksList` trên Dashboard để nhất quán (AC-05-4). Khi không overdue giữ nguyên `statusLabels` như cũ (Planned/In Progress/Done — task done không bao giờ overdue vì `isOverdue()` đã loại status `done`, AC-05-2). Vì sửa tại `TimelineItem` dùng chung, áp dụng tự động cho mọi nơi gọi `TimelineView` (chỉ có `/members/[memberId]` tại thời điểm này) — đúng ý đồ spec, không phải side-effect ngoài ý muốn.
- F-05 (FB-03) DONE: files: `app/notifications/page.tsx` — thêm state `loading` khởi tạo `true`, set `false` bên trong callback của `subscribeNotifications` (chạy đúng 1 lần đầu tiên rồi vẫn tiếp tục chạy mỗi lần có update, nhưng `setLoading(false)` gọi lại nhiều lần vô hại vì React bỏ qua set trùng giá trị — không quay lại loading ở các lần sau, AC-06-4). Trong lúc `loading === true` hiện 3 `Skeleton height={64}` (tái dùng đúng component `Skeleton` đã dùng ở Dashboard/Member Detail, không thêm pattern mới); sau khi có snapshot đầu tiên (rỗng hay có dữ liệu) mới render `NotificationList` như cũ — component này tự quyết định `EmptyState` hay danh sách dựa trên độ dài mảng, không đổi gì bên trong nó (AC-06-1..3).
- Đã tra Astryx trước khi code: `astryx component Token`, `astryx component Skeleton` — xác nhận đúng props `color`/`size` (Token) và `height` (Skeleton) đang dùng, không thêm CSS/div tay.
- Không đụng F-01/F-03 (rev 1, đã pass QC) hay bất kỳ file nào ngoài 3 phạm vi trên.
- Lint: `pnpm lint` — 0 errors, 9 warnings, tất cả đều pre-existing (không có ở 3 file vừa sửa: `app/dashboard/page.tsx` có 2 warning unused-vars `Search`/`alertCount` nhưng đã tồn tại từ rev 1, không phải do thay đổi rev 2 — verify bằng cách warning message trùng khớp dòng cũ, không liên quan tới guard `projectsLoaded` mới thêm).
- Build: `pnpm build` — thành công, 0 errors, cùng 9 warnings kể trên. Route `/dashboard` 11 kB, `/members/[memberId]` 5.04 kB, `/notifications` 5.58 kB.
- Tự đánh giá AC mới: AC-04-1, AC-04-2, AC-04-3 (guard `projectsLoaded` + fallback "No project" cho project thật sự không tồn tại) — thoả theo logic, đợi QC verify timing thật trên môi trường có network chậm. AC-05-1, AC-05-2, AC-05-3, AC-05-4 (Token đỏ/cam + label "Overdue", dùng chung `isOverdue`/ngưỡng màu với Dashboard) — thoả. AC-06-1, AC-06-2, AC-06-3, AC-06-4 (skeleton trước snapshot đầu, EmptyState/List đúng sau đó, không quay lại loading khi có update mới) — thoả theo logic.

## rev 5 — 2026-09-08 (spec rev 3: FB-CHAT-01/02/03)
- F-06 (FB-CHAT-01) DONE: files: `lib/intent.ts`, `app/api/ai/answer-query/route.ts` — thêm `hasUnfilledPlaceholder(text)` dùng pattern `\[[^\]]+\]` (đúng định nghĩa spec), đặt cạnh `isTaskCreationIntent`/`isInformationalQuery` như spec gợi ý, không tạo file mới. Áp dụng đúng 2 điểm bắt buộc trong `answer-query/route.ts`: (1) ngay khi `isTaskCreationIntent(userQuery)` true, check `userQuery` gốc trước khi gọi `extractTaskEntryFromInput` — nếu có placeholder, trả lời chung chung yêu cầu điền đủ (không phân biệt field cụ thể vì bước này chưa có `entry` để biết field nào tương ứng chỗ nào, đúng tinh thần AC-CHAT-01-1 "không cần validate riêng `[1 tiếng]` nếu đã chặn ở bước 1"); (2) sau khi có `entry`, check riêng từng field `entry.title`/`entry.projectName`/`entry.assigneeName`, liệt kê đúng tên field còn placeholder (đáp ứng AC-CHAT-02 — chỉ báo đúng field còn thiếu khi các field khác đã có tên thật). Cả 2 điểm dùng chung đúng 1 hàm, không viết regex `[...]` lần 2 (audit: `grep -n "hasUnfilledPlaceholder" app/api/ai/answer-query/route.ts` → đúng 2 lệnh gọi). Vì validate nằm ở tầng route dùng chung cho mọi lệnh có template (không rẽ nhánh theo `command.id`), tự động áp dụng cho `/assign`, `/reassign`, `/remove`, `/add`, `/log` (AC-CHAT-03) mà không cần sửa `lib/slash-commands.ts`. Không đụng `detectMemberQueryTarget` (cơ chế placeholder riêng cho `/status`, giữ nguyên) — AC-CHAT-05 (fuzzy-match tên sai chính tả, không phải placeholder) không bị ảnh hưởng vì `hasUnfilledPlaceholder` chỉ khớp `[...]`, không khớp tên gõ sai.
- F-07 (FB-CHAT-02) DONE: cùng file `app/api/ai/answer-query/route.ts` — thêm `isUnknownProjectName()` check `entry.projectName` rỗng hoặc thuộc danh sách giá trị model hay bịa (`unknown`, `không rõ`/`khong ro`, `chưa rõ`/`chua ro`, `n/a`, không phân biệt hoa thường) — chạy ngay sau khối check placeholder F-06, cùng 1 khối validate "entry đủ điều kiện tạo entry card chưa" như spec yêu cầu (không tách hàm riêng). Khi thiếu: trả câu hỏi lại nhắc tên việc/người phụ trách/thời lượng đã có (`formatEffortDuration` tái dùng từ `lib/effort.ts`, không viết lại hàm format phút→giờ), hỏi tên dự án, **không gọi `createChatLog`** (đúng rule "tránh rác chatLog dạng nháp" — khác các nhánh chặn khác vẫn log vì đó là câu trả lời hợp lệ cuối cùng, còn nhánh này chỉ là câu hỏi tạm giữa hội thoại). `effortMinutes` giữ nguyên default 60 khi không nói rõ — theo đúng giả định đã ghi sẵn trong spec (mục "Câu hỏi mở cho PM"), không tự mở rộng thêm điều kiện chặn cho effort.
- F-08 (FB-CHAT-03) DONE: files: `services/grounding.service.ts`, `app/api/ai/answer-query/route.ts` — thêm `scopeSnapshotToMember(snapshot, memberId)` cạnh `buildGroundingSnapshot`, tái dùng type `GroundingSnapshot` có sẵn (không tạo type mới). Vì `GroundingSnapshot.projects` gốc chỉ có tổng hợp toàn team (không có breakdown theo member), hàm scope **không gọi lại Firestore** mà tính lại `projects` trực tiếp từ `member.activeTasks` đã có sẵn trong snapshot (mỗi task đã có `project`/`effort`) — nhẹ hơn re-query, đúng tinh thần "tái dùng cách tính hiện có" vì cách gom nhóm theo tên dự án + cộng dồn effort giống hệt logic trong `buildGroundingSnapshot`. Áp dụng tại route: trước khi build prompt ở bước "4. Standard Q&A flow" — nếu `currentMode === "devops"`, validate `currentMemberId` khớp 1 member thật trong `allMembers` đã load sẵn ở bước 2 (không query thêm); nếu hợp lệ thì `scopeSnapshotToMember`, nếu không hợp lệ (rỗng/không khớp) trả lời "không xác định được người dùng hiện tại", **không fallback về snapshot đầy đủ** (AC-CHAT-14). Áp dụng cho **mọi** câu hỏi ở bước 4 (không rẽ theo slash command cụ thể) nên tự động cover `/report`, `/overload`, `/load`, và câu hỏi tự do (AC-CHAT-10, AC-CHAT-12). Mode `leader` không đổi gì — `scopedSnapshot = snapshot` nguyên trạng (AC-CHAT-13). Bước 3 (member-existence lookup theo tên) vẫn dùng `allMembers`/`snapshot` đầy đủ vì đó là bước tra cứu tên hợp lệ, không phải payload cuối đưa cho model ở bước 4 — không nằm trong phạm vi fix.
- Không sửa `entry-card.tsx`, `chat-box.tsx`, `lib/slash-commands.ts` — đúng "Ngoài phạm vi rev 3" (chỉ đổi logic route/service).
- Typecheck: `pnpm exec tsc --noEmit` — 1 lỗi còn lại (`services/tasks.service.ts:90`, `Record<string, unknown>` vs `FieldValue`) xác nhận **pre-existing**, không liên quan 3 file vừa sửa (verify bằng `git stash` — lỗi này (và 14 lỗi unrelated khác đang có sẵn trong working tree trước khi bắt đầu) tồn tại y hệt trước khi có thay đổi rev 3; sau khi áp lại thay đổi, số lỗi liên quan tới 3 file sửa = 0).
- Lint: `pnpm lint` — "No issues found", không có warning/error mới.
- Build: `pnpm build` — thành công, 0 errors, route `/api/ai/answer-query` build ra bình thường (136 B, dynamic).
- Không viết Playwright test ở bước này theo đúng yêu cầu (để sau khi QC PASS AC lần đầu).

## rev 6 — 2026-09-08 (fix review findings rev 3 correctness — F-06/F-07/F-08)
- Finding #2 FIXED: files: `lib/intent.ts`, `app/api/ai/answer-query/route.ts`, `components/chat/entry-card.tsx` — root cause: `isUnknownProjectName`/`UNKNOWN_PROJECT_VALUES` chỉ tồn tại local trong route, `EntryCard` Confirm chỉ check `!edited.projectName` (rỗng) nên user gõ tay "n/a"/"unknown"/"không rõ" vào ô Dự án lúc "Chỉnh sửa" vẫn bấm Confirm được, vòng qua gate F-07 (gate F-07 chỉ chạy ở lần extract đầu, không chạy lại lúc Confirm). Fix: chuyển `isUnknownProjectName` + `UNKNOWN_PROJECT_VALUES` từ route sang `lib/intent.ts` (export), route import lại dùng nguyên (không đổi hành vi), `EntryCard` import cùng hàm và đổi `isDisabled` từ `!edited.projectName` thành `isUnknownProjectName(edited.projectName)` — dùng đúng 1 nguồn sự thật ở cả client lẫn server, không copy-paste logic riêng (grep xác nhận chỉ có đúng 1 định nghĩa còn lại trong repo).
- Finding #3 FIXED: file: `lib/intent.ts` — root cause: `hasUnfilledPlaceholder` dùng `\[[^\]]+\]` khớp MỌI ngoặc vuông (kể cả `server[prod-01]`, markdown link `[chi tiết](url)`, `items[0]`), gây false positive chặn nhầm input hợp lệ có ngoặc vuông thật. Fix: build `TEMPLATE_PLACEHOLDER_LABELS` bằng cách trích xuất động các label bên trong `[...]` từ chính `template` field của `SLASH_COMMANDS` (`lib/slash-commands.ts`) qua regex `\[([^\]]+)\]` trên từng `cmd.template`, rồi ráp thành 1 regex `\[\s*(?:label1|label2|...)\s*\]` (escape ký tự đặc biệt từng label) — chỉ khớp đúng các placeholder thật đang dùng ("Tên công việc", "Tên nhân sự", "Tên dự án", "Tên task", "Tên thành viên", "Người cũ", "Người mới", "1 tiếng", "2 tiếng", "hôm nay"). Lấy list bằng cách derive từ `SLASH_COMMANDS` thay vì hardcode mảng riêng để không bao giờ lệch khỏi `lib/slash-commands.ts` khi có template mới/sửa sau này. Đã test tay 7 case (3 case review nêu: `server[prod-01]`, markdown link, `items[0]` → đều `false`; 4 case placeholder thật từ các template `/assign`,`/info`,`/reassign`,`/log` → đều `true`) bằng `npx tsx` inline script, tất cả đúng kỳ vọng.
- Finding #1 — KHÔNG fix được hoàn toàn, chỉ ghi nhận giới hạn (đúng scope): đã đọc `services/auth.service.ts`, `store/auth.store.ts`, `components/chat/chat-box.tsx`, tìm `middleware.ts`/`firebase-admin` trong toàn repo. Kết luận: **app hiện không có bất kỳ cơ chế xác thực server-side nào** cho route `/api/ai/answer-query` (hay bất kỳ route API nào khác) — Firebase Auth chỉ chạy client-side (`onAuthStateChanged` trong `lib/auth.ts`/`useAuthListener`), route Next.js nhận thẳng `memberId` từ JSON body do client tự gửi (`chat-box.tsx:336` build từ `user?.memberId` phía client) mà không có ID token nào được gửi kèm hay verify lại. Không có `firebase-admin` trong `package.json`, không có service-account credentials trong `.env.local.example`, không có `middleware.ts`. Để verify identity thật cần: thêm dependency `firebase-admin`, provision service-account key, verify `Authorization: Bearer <idToken>` ở mọi route — đây là thiết kế lại toàn bộ auth flow cho cả app, vượt quá scope rev fix 3 finding này (đúng lưu ý trong task). Quyết định: **giữ nguyên mức validate hiện có** (check `memberId` tồn tại trong danh sách member thật, không phải chuỗi rỗng/rác) vì đây đã là kiểm tra hợp lý duy nhất khả thi trong scope — không thêm được ràng buộc "khớp đúng người gọi" nếu không có identity đã-được-xác-thực từ server. Đề xuất ghi vào mục "Câu hỏi cho BA" bên dưới để BA/Tech Lead quyết định có mở scope riêng cho "server-side session verification" ở rev sau hay không.
- Typecheck: `pnpm exec tsc --noEmit` — 1 lỗi còn lại tại `services/tasks.service.ts:90` (`Record<string, unknown>` vs `FieldValue`), xác nhận **pre-existing** — file này không nằm trong diff của tôi (không xuất hiện trong `git status` các file tôi sửa), lỗi y hệt tồn tại từ trước khi tôi bắt đầu (đã có sẵn trong working tree, không do rev 6 gây ra).
- Lint: `pnpm lint` — "No issues found", không có warning/error mới.
- Không viết Playwright test ở bước này theo đúng yêu cầu.

## rev 7 — 2026-09-08 (fix ISSUE-06, ISSUE-07)
- ISS-06: FIXED — files: `lib/intent.ts`, `app/api/ai/format-entry/route.ts`, `app/api/ai/answer-query/route.ts`, `components/chat/chat-box.tsx` — root cause: F-06/F-07 chỉ được implement trong `answer-query/route.ts` (rev 5/6); nhưng tab "Log / Plan" (mode=devops) khi phát hiện `isTaskCreationIntent` lại gọi thẳng `/api/ai/format-entry`, route này gọi `extractTaskEntryFromInput` rồi trả `entry` thẳng cho client, không qua bất kỳ gate nào. Fix root-cause dùng chung: tách phần logic checkpoint 2 (list field còn placeholder trong entry) — vốn giống hệt nhau ở cả 2 route — thành hàm mới `findUnfilledPlaceholderFields(entry)` trong `lib/intent.ts`, refactor `answer-query/route.ts` gọi hàm này thay vì lặp code inline (hành vi/message giữ nguyên y hệt). Thêm vào `format-entry/route.ts` đúng 3 gate theo thứ tự giống `answer-query`: (1) `hasUnfilledPlaceholder(inputText)` chặn trước khi extract, (2) `findUnfilledPlaceholderFields(entry)` chặn sau khi extract nếu field nào còn `[...]`, (3) `isUnknownProjectName(entry.projectName)` chặn nếu dự án rỗng/bịa — cả 3 hàm tái dùng nguyên từ `lib/intent.ts`, không viết lại logic. Đổi response shape khi bị chặn thành `{ chatLogId, entry: null, message }` (field name giữ nguyên, chỉ thêm giá trị `null` hợp lệ cho `entry`) để client phân biệt được "đã tạo entry card" (entry có giá trị) và "bị chặn, chỉ có cảnh báo" (entry = null). Sửa `chat-box.tsx` nhánh gọi `/api/ai/format-entry`: chỉ `appendMessage(..., role: "ai-entry", ...)` khi `entry` khác `null`, ngược lại chỉ hiện `message` dưới dạng `ai-answer` — không đổi luồng thành công hiện có (khi có `entry` thật, hành vi y hệt trước).
- ISS-07: FIXED — files: `app/api/ai/answer-query/route.ts`, `components/chat/chat-box.tsx` — root cause: field `mode` bị dùng lẫn cho 2 khái niệm (tab hội thoại đang mở quyết định route/UI, và vai trò thật quyết định F-08 scoping); tab "Ask" trong `ModeToggle`/`useChatStore` luôn gán `mode="leader"` bất kể `user.role` thật là gì, nên `answer-query/route.ts` không bao giờ kích hoạt `scopeSnapshotToMember` khi 1 tài khoản DevOps dùng tab "Ask". Fix root-cause: tách 2 khái niệm theo đúng gợi ý QC — giữ nguyên `mode` cho nhiệm vụ cũ (chọn route/slash-command/lưu lịch sử chat theo tab), thêm field mới `userRole` gửi kèm khi gọi `/api/ai/answer-query`, lấy trực tiếp từ `user?.role` trong `useAuthStore` (đã có sẵn, được cập nhật qua `setRole` khi người dùng đổi vai trò, độc lập với tab đang mở — xác nhận qua đọc `store/auth.store.ts`, `types/user.ts`). Trong route, thêm `effectiveRole = userRole || currentMode` (fallback về `mode` nếu client cũ chưa gửi `userRole`, không breaking change) và đổi mọi chỗ dùng `currentMode === "devops"` để quyết định F-08 (cả điều kiện kích hoạt `scopeSnapshotToMember` lẫn `currentMemberId` mặc định) sang dùng `effectiveRole === "devops"`. Không sửa `format-entry/route.ts` vì route đó không có khái niệm "xem dữ liệu toàn team" (chỉ tạo entry từ input của chính người gõ, không liên quan F-08).
- Typecheck: `pnpm exec tsc --noEmit` — 1 lỗi còn lại tại `services/tasks.service.ts:90` (`Record<string, unknown>` vs `FieldValue`) — xác nhận pre-existing, không liên quan tới 4 file vừa sửa (file này không nằm trong diff của rev này).
- Lint: `pnpm lint` — "No issues found".
- Build: `pnpm build` — thành công, 0 errors, route `/api/ai/format-entry` và `/api/ai/answer-query` build bình thường.
- Không viết Playwright test ở bước này theo đúng yêu cầu.

## rev 8 — 2026-09-08 (fix ISSUE-06 reopened — F-07 chưa validate project thật)
- ISS-06: FIXED (lại) — files: `lib/intent.ts`, `app/api/ai/format-entry/route.ts`, `app/api/ai/answer-query/route.ts` — root cause (khác lần fix rev 7): `isUnknownProjectName` chỉ so khớp `projectName` với 1 danh sách sentinel cố định ("unknown", "không rõ", "n/a"...). Khi input không nêu dự án, `extractTaskEntryFromInput` (model AI) tự bịa ra 1 chuỗi tên dự án nghe hợp lý (ví dụ "Core Platform") thay vì trả rỗng/"unknown" — chuỗi này không rỗng và không khớp sentinel nên gate F-07 không kích hoạt, entry card vẫn được tạo với dự án bịa (QC vòng 2 case 3 FAIL, dự án thật duy nhất trong hệ thống là "LineFX"). Fix: thêm hàm mới `isRealProjectName(projectName, realProjectNames)` trong `lib/intent.ts` — validate bằng cách so khớp `projectName` với danh sách project THẬT (case-insensitive, bỏ dấu, substring 2 chiều — tái dùng đúng kiểu normalize của `findMemberByName` trong `services/members.service.ts`, không viết thuật toán match mới); trả `false` ngay nếu `isUnknownProjectName` đúng (rỗng/sentinel — giữ nguyên hành vi cũ) hoặc nếu không khớp bất kỳ tên project thật nào. Cả `format-entry/route.ts` và `answer-query/route.ts` gọi `getProjects()` (`services/projects.service.ts`, hàm có sẵn, không viết mới) đúng 1 lần tại điểm có `entry` rồi gọi `isRealProjectName(entry.projectName, realProjects.map(p => p.name))` thay `isUnknownProjectName(entry.projectName)` trực tiếp — không gọi trùng Firestore: `format-entry` route trước đó chưa từng fetch project; `answer-query` route có gọi `getProjects()` gián tiếp qua `buildGroundingSnapshot()` nhưng ở bước 2, nằm SAU nhánh task-creation-intent return sớm ở bước 1 nên không trùng lệnh gọi nào trong cùng 1 request. Không đổi signature `isUnknownProjectName` (vẫn nhận 1 tham số, không async) nên `components/chat/entry-card.tsx` (dùng hàm này cho nút Xác nhận, client component không gọi Firestore được) không cần sửa gì — vẫn chặn đúng rỗng/sentinel khi user tự gõ tay lúc edit, còn case bịa tên dự án đã bị chặn từ server trước khi entry card có cơ hội hiển thị.
- Đã tự trace lại 3 case bằng script Node độc lập (áp cùng logic `isRealProjectName`) trước khi tin tưởng: `LineFX` (dự án thật) → `true` (pass); `Core Platform` (bịa) → `false` (bị chặn); `unknown`/`""` (sentinel cũ) → `false` (vẫn bị chặn như cũ); `linefx`/`LineFX v2` (case-insensitive/fuzzy nhẹ) → `true`.
- Typecheck: `pnpm exec tsc --noEmit` — 1 lỗi còn lại tại `services/tasks.service.ts:90` (`Record<string, unknown>` vs `FieldValue`), xác nhận pre-existing qua `git stash` (baseline có 15 lỗi rải rác nhiều file không liên quan, không phải do rev này) — file này không nằm trong diff.
- Lint: `pnpm lint` — "No issues found".
- Build: `pnpm build` — thành công, 0 errors, đủ route (bao gồm `/api/ai/answer-query`, `/api/ai/format-entry`).
- Không viết Playwright test ở bước này theo đúng yêu cầu.

## rev 9 — 2026-09-08 (Playwright regression test cho spec rev 3, sau khi QC PASS)
- Viết `e2e/chat-ai-rev3.spec.ts` — 5 test case tái hiện đúng Given/When/Then của các AC `must` mà QC đã verify PASS ở vòng "QC vòng 3 — verify ISSUE-06 lần cuối" + "QC vòng 2" (ISSUE-07 CLOSED): AC-CHAT-01-1 (template `[...]` nguyên văn bị chặn), AC-CHAT-06 (giao task tự nhiên thiếu dự án → AI hỏi lại, không bịa "Core Platform"/"Unknown"), AC-CHAT-04/08 (case hợp lệ đủ trường vẫn tạo entry card bình thường — đối chứng không bị 2 gate trên chặn nhầm), AC-CHAT-10/12 (DevOps + tab "Ask" + `/report` không lộ tên/effort member khác), AC-CHAT-13 (Leader + tab "Ask" + `/report` vẫn thấy toàn team — đối chứng không regression). Không viết test riêng cho AC-CHAT-02/03/05/07/09/11/14 (biến thể nhỏ của cùng 1 root cause/cùng cơ chế đã cover, hoặc khó tái hiện ổn định qua UI thật như AC-CHAT-14 theo đúng ghi chú của QC).
- Môi trường test: app này không có Firebase emulator/test doctor riêng — mọi test chạy thẳng vào dev server thật (`playwright.config.ts` trỏ `baseURL: http://localhost:3001`, đổi từ `3000` vì cổng 3000 đang bị 1 tiến trình cũ chiếm nhưng không phục vụ app, khớp với cổng QC đã dùng trong `qc-report.md`) + Firestore thật (không phải emulator) — không có cách nào khác trong scope hiện tại của repo để cô lập. Dùng "Continue as guest" (`signInAnon`, có sẵn từ ISS-02) làm cách đăng nhập ổn định cho Playwright; xác nhận qua thực nghiệm rằng session guest trong môi trường này gắn với 1 tài khoản cố định đã có sẵn dữ liệu thật từ các vòng QC trước (project "LineFX", members "Bảo Dương 2005"/"Bao Duong 98", role Leader mặc định, có nút "Đổi sang DevOps"/"Đổi sang Leader" ở sidebar để chuyển vai trò) — test tái dùng đúng dữ liệu này thay vì tạo mới, không gọi API "Quản lý dữ liệu / Reset" (`/api/dev/data`, action `reset`) vì thao tác đó xoá luôn collection `users` (sẽ phá vỡ session/role đang dùng chung với QC), không phù hợp làm test fixture cho môi trường chia sẻ này.
- Helper trong test file: `ensureLeaderRole`/`ensureDevopsRole` (dùng đúng nút toggle role có sẵn ở sidebar, không tự ghi Firestore trực tiếp), `openChatOnModeRadio` (chọn đúng tab "Log / Plan"/"Ask" qua `SegmentedControl`, đợi lịch sử chat theo mode load xong), `sendMessage` (gõ + Enter, đợi bubble "Thinking" biến mất — timeout 90s vì gọi AI thật qua NVIDIA gateway, đã đo thực tế 11–28s/lượt). Test đọc đúng `article` cuối cùng (`ChatMessageList` render mỗi message thành `role="article"`, kể cả bubble "Thinking") để tránh false-positive từ lịch sử chat cũ rất dài đã tích luỹ qua nhiều vòng QC thủ công trên cùng tài khoản (không xoá lịch sử cũ — chỉ đọc message mới nhất, không đọc toàn bộ `body.innerText()`).
- Chạy `pnpm exec playwright test e2e/chat-ai-rev3.spec.ts` 2 lần liên tiếp, cả 2 lần 5/5 pass (không flaky) — dùng `test.describe.configure({ mode: "serial" })` vì cả 5 test share chung 1 tài khoản/role-toggle, chạy song song sẽ đụng độ.
- Không sửa code chức năng nào, chỉ thêm file test + đổi `baseURL` trong `playwright.config.ts` (hạ tầng test, không phải logic sản phẩm).
- `pnpm lint`: "No issues found".

## Câu hỏi cho BA
- Rev 6 (review finding #1, F-08 devops scoping): app hiện **không có cơ chế xác thực server-side** cho bất kỳ route API nào (không middleware, không verify ID token, không firebase-admin) — mọi route tin thẳng field client gửi lên trong body JSON. Fix triệt để finding #1 (ràng `memberId` với danh sách đúng người gọi thật) đòi hỏi thêm `firebase-admin` + service-account + verify `Authorization` header ở tầng route — là thay đổi kiến trúc auth cho toàn app, không chỉ riêng route chat. Đã giữ nguyên mức validate "memberId tồn tại trong danh sách member" (mức tốt nhất khả thi trong scope hiện tại) và cần BA/Tech Lead quyết định có mở 1 rev riêng cho "server-side session verification" hay chấp nhận rủi ro này (tương tự finding #4 đã được chấp nhận trong review-notes).
- Không có câu hỏi mới ngoài 2 câu hỏi mở đã ghi sẵn trong `spec.md` (mục "Câu hỏi mở cho PM"). Đã làm theo đúng giả định của spec: dedup theo `relatedTaskId` duy nhất (không báo lại khi "tái quá hạn"), và chấp nhận giới hạn không có cron (notification chỉ sinh khi có người mở Dashboard).
- Rev 3 (Chat AI): làm đúng theo 2 giả định đã có sẵn trong spec — (1) effort/thời lượng vẫn default 60 phút khi không nói rõ, chỉ dự án bắt buộc hỏi lại (FB-CHAT-02); (2) thu hẹp dữ liệu áp dụng cho **mọi** câu hỏi ở mode devops, không riêng `/report` (FB-CHAT-03). Không phát sinh câu hỏi mới ngoài 2 điểm BA đã tự nêu.

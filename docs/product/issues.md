# Issues — rev 1

## ISSUE-01: Notification `overdue_task` bị tạo trùng khi Firestore emit nhiều snapshot liên tiếp
- **AC liên quan**: AC-02-2
- **Severity**: high
- **Status**: fixed
- **Cách fix**: `services/notifications.service.ts` — `createNotification(input, id?)` giờ luôn dùng `setDoc(..., { merge: true })` thay vì `addDoc`; khi có `id` truyền vào thì ghi vào doc id xác định thay vì auto-id. `app/dashboard/page.tsx` gọi `createNotification(input, \`overdue_task_${task.id}\`)`. Nhờ vậy dù effect chạy lại nhiều lần do nhiều snapshot liên tiếp (hoặc nhiều tab), mọi lần gọi cho cùng task đều ghi đè lên đúng 1 doc — không còn phụ thuộc vào việc đọc lại `notifications` qua closure để chống trùng, chống race kể cả cross-tab.
- **Mô tả**: Dedup effect trong `app/dashboard/page.tsx` (~dòng 109-135) chỉ depend `[overdueTasks]`, đọc `notifications` qua closure (có `eslint-disable-next-line react-hooks/exhaustive-deps`). Khi `tasks` emit nhiều snapshot liên tiếp (Firestore local-cache rồi server-ack) trước khi subscription `notifications` kịp phản ánh notification vừa ghi, effect chạy lại với `notifications` cũ → tạo `Notification` thứ 2 cho cùng `relatedTaskId`.
- **Bằng chứng**: QC xác nhận trực tiếp qua Firestore — `task-phoenix-1` có 2 notification `overdue_task` tạo cách nhau 1ms; 2 task QC seed thêm cũng bị trùng.
- **Bước tái hiện**: Có task quá hạn chưa từng có notification → load Dashboard → Firestore emit 2 snapshot tasks gần nhau → 2 notification `overdue_task` cho cùng task được tạo.
- **Gợi ý fix**: dedup nên chống race — ví dụ dùng ref/set in-memory đánh dấu "đang xử lý/đã xử lý" theo `taskId` ngay khi bắt đầu gọi `createNotification` (không đợi round-trip subscription), hoặc kiểm tra lại trong Firestore bằng query trước khi ghi (nếu cần chống trùng cross-tab/cross-session), hoặc constraint ở tầng ghi (doc id xác định = `overdue_task_<taskId>` thay vì auto-id, dùng `setDoc` để ghi đè thay vì `addDoc` tạo mới).

## ISSUE-02: Cần thêm lựa chọn đăng nhập anonymous để test/dev không cần Google OAuth thật
- **AC liên quan**: N/A (hạ tầng test, không phải AC của spec) — chặn QC hoàn thành AC-01/02/03 còn lại
- **Severity**: high (blocker cho QC)
- **Status**: fixed
- **Cách fix**: thêm `signInAnon()` trong `services/auth.service.ts` (dùng `signInAnonymously` từ Firebase Auth, cùng pattern `signInWithGoogle`). Thêm nút "Continue as guest" trong `app/login/page.tsx` gọi hàm này song song với nút Google hiện có, không đổi luồng Google. Đã đọc kỹ `linkOrCreateUser` — không cần sửa: `firebaseUser.email` dùng `?? ""`, `displayName` dùng `?? email`, `photoURL` dùng `?? null`, đều đã xử lý `null`/`undefined` an toàn cho user ẩn danh (không có email/displayName thật), không crash.
- **Mô tả**: `services/auth.service.ts` chỉ có `signInWithPopup(auth, googleProvider)`. QC/sandbox browser không có Google session thật nên không vượt qua được `/login`, không test được UI Dashboard. User (PM/owner) đã bật Anonymous Authentication trên Firebase Console.
- **Yêu cầu**: Thêm lựa chọn đăng nhập ẩn danh (Firebase `signInAnonymously`) song song với Google — ví dụ 1 nút "Continue as guest" / "Đăng nhập ẩn danh" trên `app/login/page.tsx`, gọi hàm mới trong `services/auth.service.ts` (theo đúng pattern hàm Google hiện có). Không thay đổi luồng Google hiện tại.
- **Lưu ý**: `linkOrCreateUser` (gọi khi sign-in) cần hoạt động đúng với `User` ẩn danh của Firebase (không có email/displayName thật) — kiểm tra không crash khi các field đó là `null`/`undefined`.

## ISSUE-03: Giao diện có dấu hiệu lỗi CSS
- **AC liên quan**: N/A (báo cáo chung từ PM, không gắn AC cụ thể)
- **Severity**: medium
- **Status**: fixed
- **Kết quả kiểm tra**: đã kiểm tra `/login`, `/dashboard`, `/members`, `/members/[memberId]`, `/members/new`, `/chat`, `/notifications` trên dev server (localhost:3000, đăng nhập bằng lối ẩn danh mới thêm ở ISSUE-02). Tất cả 7 route trả HTTP 200, HTML SSR có `data-astryx-theme="devops-tracker"` và `<link rel="stylesheet" href="/_next/static/css/app/layout.css">` đúng — `astryx.css`/`reset.css` import đúng qua `app/globals.css` (dòng 1-3), không thiếu. Không tìm thấy CSS bị vỡ ở các trang/route đã kiểm tra. Phát hiện duy nhất: `/login` từng trả 500 với lỗi `Cannot find module './171.js'` — xác định đây là stale `.next` build cache (webpack chunk hash lệch sau nhiều lần sửa file mà dev server cũ không restart), không phải lỗi source code; đã tự hết sau `rm -rf .next` + restart dev server, các route sau đó đều 200 bình thường. Không sửa gì thêm vì không tìm thấy nguyên nhân CSS cụ thể nào trong source — các vi phạm token/hex/px rải rác trong `member-card.tsx`, `workload-matrix.tsx`, `team-timeline-chart.tsx`, `project-allocation-grid.tsx`, `app/projects/page.tsx`, `floating-chat.tsx`, `chat-box.tsx` đều là code có từ trước (không thuộc phạm vi rev "Overdue Task Alerts"), không phải style vỡ mà chỉ là chưa dùng token Astryx thuần — nằm ngoài scope 3 issue này, không tự ý sửa.
- **Mô tả**: User (PM) quan sát thấy giao diện app có vẻ đang bị lỗi CSS (chưa có chi tiết cụ thể trang/vùng nào). Cần Developer tự kiểm tra tổng thể các trang chính (dashboard, members, chat, notifications, login) xem có style vỡ, thiếu token, hoặc Astryx CSS chưa load đúng hay không, và báo lại phát hiện + fix nếu tìm thấy nguyên nhân rõ ràng.

## ISSUE-04: Notification `overdue_task` trùng lặp còn tồn đọng trong Firestore từ trước khi fix ISS-01 (dữ liệu rác chưa dọn)
- **AC liên quan**: AC-02-2
- **Severity**: minor
- **Status**: closed
- **QC verify (vòng 3)**: Vào `/notifications` trên UI thực — `qc-task-overdue-5d`, `qc-task-overdue-1d`, `task-phoenix-1` mỗi task chỉ còn đúng 1 notification `overdue_task`, không còn cặp trùng nào. Không có console error.
- **Cách fix**: viết script one-off (`scripts/_cleanup-dup-overdue-notifications.ts`, dùng cùng cách kết nối Firestore REST API như `scripts/seed.ts`) để quét toàn bộ `notifications` collection, gom theo `relatedTaskId` cho `type === "overdue_task"`, giữ lại bản có doc id đúng format `overdue_task_<taskId>` (nếu có) hoặc bản `createdAt` cũ nhất, xoá các bản còn lại. Chạy script và kiểm tra trực tiếp Firestore (cả REST list lẫn `runQuery` structured query để loại trừ phân trang/cache): tại thời điểm chạy, `notifications` collection chỉ có 6 doc tổng, đúng 3 doc `type === "overdue_task"` — mỗi `relatedTaskId` (`qc-task-overdue-5d`, `qc-task-overdue-1d`, `task-phoenix-1`) chỉ còn đúng 1 bản, không phát hiện cặp trùng nào còn tồn tại nữa (script log "Deleted 0 duplicate" — dữ liệu rác mô tả trong issue đã không còn tại thời điểm fix, có thể đã được dọn/ghi đè giữa lúc QC ghi nhận issue và lúc fix chạy). Đã xoá file script khỏi repo sau khi chạy xong (one-off, không phải code lâu dài).
- **Mô tả**: Fix của ISS-01 (deterministic doc id `overdue_task_<taskId>` + `setDoc merge`) hoạt động đúng cho mọi notification được tạo **sau khi fix** — QC đã verify bằng 8+ lần reload Dashboard liên tiếp (kể cả dồn dập) trên nhiều task, không phát sinh trùng mới. Tuy nhiên, khi QC vòng 2 mở trang Notifications lần đầu, phát hiện 3 task (`qc-task-overdue-5d`, `qc-task-overdue-1d`, `task-phoenix-1`) mỗi task có **2** notification `overdue_task` với **doc id ngẫu nhiên kiểu `addDoc`** (ví dụ `ef4pBsf1rmzd1RIdg2yM`, `zyrgevhlc5KsOQm5QOrA` — không phải `overdue_task_<taskId>`), timestamp cách nhau vài trăm ms, được tạo **trước khi dev server hiện tại khởi động** (tức là dữ liệu tồn đọng từ lần chạy code cũ, có thể là chính lúc Developer/QC vòng 1 phát hiện ra ISS-01, trước khi fix được áp dụng). Dữ liệu rác này vẫn hiển thị trên trang Notifications thật, khiến PM thấy 2 thông báo giống hệt nhau cho cùng 1 task — vi phạm trực tiếp yêu cầu "tổng số notification cho task đó vẫn là 1" của AC-02-2 ở trạng thái hiện tại của hệ thống, dù root cause logic đã được fix.
- **Bằng chứng**: Query Firestore trực tiếp (`notifications` collection, `type == "overdue_task"`) tại thời điểm bắt đầu QC vòng 2 cho thấy 6 doc cho 3 `relatedTaskId` (2 doc/task), toàn bộ đều có `createdAt` sớm hơn thời điểm dev server phiên QC này khởi động (xác nhận qua `ps aux` so với timestamp `createdAt`).
- **Bước tái hiện**: Mở `/notifications` trên dữ liệu Firestore hiện tại (chưa dọn) → thấy "QC Overdue 5 days task đã quá hạn" xuất hiện 2 lần với cùng nội dung, cùng giờ tạo (chênh nhau ~200ms); tương tự cho "QC Overdue 1 day task" và "Rebuild pipeline caching layer".
- **Gợi ý fix**: chạy 1 lần migration/cleanup script xoá các doc `overdue_task` trùng `relatedTaskId` (giữ lại 1 bản, ưu tiên bản có doc id đúng format `overdue_task_<taskId>` nếu có, hoặc bản cũ nhất), không cần sửa code logic (logic hiện tại đã đúng cho notification mới).

## ISSUE-06: F-06/F-07 (placeholder & unknown-project guard) không chạy khi Leader giao task qua tab "Log / Plan" — chỉ hoạt động ở tab "Ask"
- **AC liên quan**: AC-CHAT-01-1, AC-CHAT-02, AC-CHAT-03, AC-CHAT-06, AC-CHAT-09
- **Severity**: high (blocker cho rev 3 — đây chính là luồng UI thật mà Leader dùng để giao task, không phải trường hợp phụ)
- **Status**: closed
- **QC verify (vòng 3, 2026-09-08)**: Gõ tay trên UI thật (`localhost:3001/chat`, role Leader, tab "Log / Plan") đúng case đã FAIL ở vòng 2 (case 3): `Giao task kiểm tra log server cho Bảo Dương 2005, thời gian 1 tiếng` (không nêu dự án) → **PASS**. AI trả lời "Đã ghi nhận: giao **Kiểm tra log server** cho **Bảo Dương 2005**. Bạn cho biết task này thuộc **dự án nào**?" — hỏi lại đúng như kỳ vọng, KHÔNG bịa tên dự án ("Core Platform" không còn xuất hiện ở lần gửi mới), KHÔNG tạo entry card mới cho input này. Regression check case đã pass trước đó: `Giao task kiểm tra log server rev3-qc cho Bảo Dương 2005 thuộc dự án LineFX thời gian 1 tiếng` (nêu đúng dự án thật "LineFX") → entry card vẫn tạo bình thường với `projectName="LineFX"`, `Assignee: Bảo Dương 2005`, không bị chặn nhầm. Không có console error ở cả 2 lần gửi. Đóng issue.
- **Cách fix (vòng 2 — khác lần fix trước)**: lần fix trước (dev-log rev trước) chỉ thêm đúng vị trí gọi `isUnknownProjectName` vào `format-entry/route.ts` — hàm đó chỉ so khớp `projectName` với 1 danh sách sentinel cố định ("unknown", "không rõ", "n/a"...), nên khi model AI tự bịa ra 1 tên dự án nghe hợp lý (ví dụ "Core Platform") thay vì trả về rỗng/"unknown", gate không bắt được (QC vòng 2 case 3 FAIL). Lần fix này thêm hàm mới `isRealProjectName(projectName, realProjectNames)` trong `lib/intent.ts` — validate `projectName` bằng cách so khớp (case-insensitive, bỏ dấu, substring 2 chiều — tái dùng đúng kiểu normalize của `findMemberByName` trong `services/members.service.ts`, không viết thuật toán mới) với danh sách project THẬT lấy từ `getProjects()` (`services/projects.service.ts`, đã có sẵn, dùng lại nguyên hàm). `isUnknownProjectName` giữ nguyên không đổi (vẫn dùng trong `isRealProjectName` làm bước loại nhanh giá trị rỗng/sentinel, và vẫn dùng nguyên trạng ở `components/chat/entry-card.tsx` cho nút Xác nhận — client component này không gọi Firestore nên không đổi được sang async, chỉ cần chặn rỗng/sentinel khi user tự sửa tay là đủ vì case bịa tên đã bị chặn từ server trước khi entry card được tạo). Cả `app/api/ai/format-entry/route.ts` và `app/api/ai/answer-query/route.ts` đều gọi `getProjects()` một lần tại đúng điểm có `entry` (không trùng lặp: `format-entry` route trước đó chưa từng gọi `getProjects`; `answer-query` route có gọi `buildGroundingSnapshot()`→`getProjects()` ở bước 2 nhưng bước đó nằm SAU nhánh task-creation-intent return sớm ở bước 1, nên không có lệnh gọi trùng nào bị bỏ qua) rồi gọi `isRealProjectName(entry.projectName, realProjects.map(p => p.name))` thay cho `isUnknownProjectName(entry.projectName)` trực tiếp. Không đổi signature `isUnknownProjectName` nên `entry-card.tsx` không cần sửa gì.
- **QC verify vòng 2 (2026-09-08) — để tham khảo, đã fix thêm sau đó**:
- **QC verify vòng 2 (2026-09-08)**: Test lại đúng 4 bước trong "Cách fix"/"Bước tái hiện" qua tab "Log / Plan" (role Leader), gõ tay trên UI thật (`localhost:3001/chat`):
  1. `Giao task [Tên công việc] cho [Tên nhân sự] thuộc dự án [Tên dự án] thời gian [1 tiếng]` → **PASS**. Bị từ chối đúng với "⚠️ Nội dung vẫn còn chỗ trống chưa điền", không tạo entry card.
  2. `Giao task [Tên công việc] cho Bảo Dương 2005 thuộc dự án LineFX thời gian 1 tiếng` (1 field còn placeholder) → **PASS**. Bị từ chối đúng, không tạo entry card.
  3. `Giao task kiểm tra log server cho Bảo Dương 2005, thời gian 1 tiếng` (không nêu dự án) → **FAIL — còn tái hiện y hệt bug gốc**. Entry card vẫn được tạo ngay với `title="Kiểm tra log server"`, `projectName="Core Platform"` (dự án bịa, không tồn tại — dự án thật duy nhất là "LineFX"), `Assignee: Bảo Dương 2005`, nút "Xác nhận" khả dụng. AI không hỏi lại "task này thuộc dự án nào?". Root cause: `extractTaskEntryFromInput` (gọi NVIDIA/AI service) tự bịa ra chuỗi "Core Platform" thay vì trả về rỗng/"unknown" khi input không nêu dự án — `isUnknownProjectName("Core Platform")` trả về `false` vì "Core Platform" không rỗng và không khớp danh sách `UNKNOWN_PROJECT_VALUES` ("unknown", "không rõ", ...). Checkpoint F-07 mới thêm vào `format-entry/route.ts` đúng là có chạy (đã verify code), nhưng nó chỉ bắt được các giá trị "unknown" nghĩa đen — không bắt được trường hợp model tự bịa một cái tên dự án nghe hợp lý nhưng không có thật trong hệ thống. Đây là đúng root cause mà chính issue gốc (dòng "Actual" bên dưới) đã cảnh báo trước: "nên `isUnknownProjectName` cũng sẽ không bắt được kể cả nếu gate này có chạy".
  4. `Giao task kiểm tra log server[prod-01] cho Bảo Dương 2005 thuộc dự án LineFX thời gian 1 tiếng` (regression check, ngoặc vuông thật) → **PASS**. Entry card tạo đúng `projectName="LineFX"`, không bị chặn nhầm.
  - Không có console error trong toàn bộ 4 lần test. Screenshot case 3 (fail): `docs/product/qc-screenshots/issue06-case3-fail-rev-qc2.png`.
  - **Kết luận**: 3/4 case đã fix đúng (case 1, 2, 4). Case 3 (F-07 — chặn dự án không có thật/tự bịa) **chưa fix triệt để** — gate `isUnknownProjectName` chỉ nhận diện đúng các giá trị "unknown" tường minh, không nhận diện được khi AI tự sinh ra một tên dự án hợp lý nhưng không khớp bất kỳ project thật nào trong hệ thống. Cần thêm bước validate `entry.projectName` so với danh sách project thật (ví dụ so khớp với `getProjects()`/danh sách project đang có trong Firestore) thay vì chỉ so khớp chuỗi "unknown" cố định — nếu không khớp project thật nào thì coi như unknown và hỏi lại, thay vì tin tưởng bất kỳ chuỗi non-empty nào model trả về.
- **Cách fix (dev-log, chưa đủ)**: thêm đúng 2 checkpoint vào `app/api/ai/format-entry/route.ts`, tái dùng nguyên hàm từ `lib/intent.ts` — không viết lại logic: (1) checkpoint 1 — `hasUnfilledPlaceholder(inputText)` chặn trước khi gọi `extractTaskEntryFromInput` nếu input còn `[...]` chưa điền; (2) checkpoint 2 — sau khi có `entry`, chặn nếu bất kỳ field nào (`title`/`projectName`/`assigneeName`) còn placeholder; (3) F-07 — chặn nếu `isUnknownProjectName(entry.projectName)` đúng (rỗng hoặc giá trị "unknown"/"không rõ"...). Để tránh 2 route lệch nhau lần nữa, tách phần logic identical giữa 2 route (tính danh sách field còn placeholder cho checkpoint 2) thành 1 helper dùng chung mới `findUnfilledPlaceholderFields(entry)` trong `lib/intent.ts`, refactor luôn `answer-query/route.ts` để gọi helper này thay vì lặp lại code — cả 2 route giờ chạy đúng cùng 1 logic checkpoint 2. Checkpoint 1 (`hasUnfilledPlaceholder`) và F-07 (`isUnknownProjectName`) vốn đã là hàm dùng chung sẵn, chỉ cần gọi thêm ở `format-entry/route.ts`, không cần tách thêm. Vì response shape của `format-entry` khi chặn không còn `entry` thật (client cần phân biệt "đã tạo entry card" và "bị chặn, chỉ có cảnh báo"), đổi response khi bị chặn thành `{ chatLogId, entry: null, message }` (giữ nguyên field name `entry`/`message` client đang đọc, chỉ thêm giá trị `null` hợp lệ) và sửa `components/chat/chat-box.tsx` (nhánh gọi `/api/ai/format-entry`) để chỉ append `ai-entry` message khi `entry` khác `null`, nếu không chỉ hiện `message` dưới dạng `ai-answer` — không phá vỡ luồng thành công hiện có.
- **Mô tả**: `components/chat/chat-box.tsx` dòng ~310: khi `mode === "devops"` (đây là giá trị mà tab **"Log / Plan"** trong `ModeToggle` set — `SegmentedControlItem value="devops" label="Log / Plan"`) và `isTaskCreationIntent(currentText)` đúng, request được gửi tới `/api/ai/format-entry`, **không phải** `/api/ai/answer-query`. Toàn bộ fix F-06 (`hasUnfilledPlaceholder`, 2 checkpoint) và F-07 (`isUnknownProjectName`) theo dev-log rev 5/6 chỉ được thêm vào `app/api/ai/answer-query/route.ts` — `app/api/ai/format-entry/route.ts` gọi thẳng `extractTaskEntryFromInput` rồi trả `entry` về client, không có bất kỳ validate nào. Vì tab "Log / Plan" là nơi tự nhiên nhất để gõ "Giao task ... cho ... thuộc dự án ... thời gian ...", đây chính là luồng mà AC-CHAT-01-1/02/03/06/09 mô tả — nhưng nó đi qua route hoàn toàn không có gate F-06/F-07.
- **Bước tái hiện (AC-CHAT-01-1)**: 1. Đăng nhập Leader. 2. Vào `/chat`, chọn tab **"Log / Plan"**. 3. Gõ nguyên văn `Giao task [Tên công việc] cho [Tên nhân sự] thuộc dự án [Tên dự án] thời gian [1 tiếng]`. 4. Gửi.
- **Expected**: AI từ chối, không tạo entry card, yêu cầu điền cụ thể (giống hệt khi test qua tab "Ask" — đã verify tab "Ask" trả đúng "⚠️ Nội dung vẫn còn chỗ trống chưa điền...").
- **Actual**: AI tạo entry card với `title = "[Tên công việc]"`, `projectName = "[Tên dự án]"`, tự động đề xuất "DevOps Engineer" thay cho `[Tên nhân sự]` (hành vi fallback dành cho "tên có thật nhưng gõ sai" — bị áp dụng nhầm cho placeholder chưa điền, đúng behavior sai mà F-06 rule nghiệp vụ cấm), có nút "Xác nhận" khả dụng.
- **Bước tái hiện (AC-CHAT-02)**: Cùng tab "Log / Plan", gõ `Giao task [Tên công việc] cho Bảo Dương 2005 thuộc dự án LineFX thời gian 1 tiếng` (chỉ 1 field còn placeholder).
- **Actual**: Entry card được tạo với `title = "Tên công việc"` (AI tự bóc dấu ngoặc vuông nhưng giữ nguyên placeholder text làm tiêu đề task thật), `projectName = "LineFX"`, assignee đúng "Bảo Dương 2005", nút "Xác nhận" khả dụng — không có bất kỳ cảnh báo nào về field còn thiếu.
- **Bước tái hiện (AC-CHAT-06/F-07)**: Cùng tab "Log / Plan", gõ `Giao task kiểm tra log server cho Bảo Dương 2005, thời gian 1 tiếng` (không nêu dự án).
- **Expected**: AI hỏi lại "task này thuộc dự án nào?", không tạo entry card.
- **Actual**: Entry card được tạo ngay với `projectName = "Core Platform"` — một dự án không tồn tại trong hệ thống (dự án thật duy nhất trong dữ liệu hiện tại là "LineFX", xem Dashboard) — AI đã tự bịa một cái tên nghe hợp lý thay vì hỏi lại hoặc trả "Unknown" (nên `isUnknownProjectName` cũng sẽ không bắt được kể cả nếu gate này có chạy, nhưng vấn đề chính vẫn là gate hoàn toàn không chạy trên route `format-entry`).
- **Screenshot**: đã chụp full-page cho cả 2 case AC-CHAT-01-1 và AC-CHAT-06/F-07 trong phiên QC (không lưu file, xem lại bằng cách tái hiện theo bước trên — console không có lỗi JS trong cả hai trường hợp).
- **Gợi ý fix**: áp dụng đúng 2 checkpoint `hasUnfilledPlaceholder` (F-06) + `isUnknownProjectName` (F-07) vào `app/api/ai/format-entry/route.ts` sau khi có `entry` từ `extractTaskEntryFromInput`, y hệt logic đã có trong `answer-query/route.ts` — tái dùng đúng 2 hàm hiện có trong `lib/intent.ts` (đã export sẵn), không viết lại logic. Cân nhắc gộp chung 1 helper dùng cho cả 2 route (`answer-query` và `format-entry`) để tránh lệch nhau lần nữa nếu 1 trong 2 route đổi sau này — đúng tinh thần "root cause dùng chung" mà spec rev 3 yêu cầu (spec chỉ định "sửa tại `answer-query/route.ts`" vì lúc viết spec chưa xác định `format-entry` cũng là 1 lối vào task creation intent).

## ISSUE-07: F-08 (thu hẹp dữ liệu theo mode devops) không áp dụng khi dùng tab "Ask" — tab này luôn gửi `mode: "leader"` bất kể vai trò đang đăng nhập là DevOps
- **AC liên quan**: AC-CHAT-10, AC-CHAT-12 (khi dùng tab "Ask")
- **Severity**: high (lộ dữ liệu effort/task toàn team cho tài khoản DevOps — đúng lỗ hổng US-06/FB-CHAT-03 vốn phải chặn)
- **Status**: closed
- **QC verify vòng 2 (2026-09-08)**: Test lại đúng 4 bước trong "Bước tái hiện"/"Đối chứng" trên UI thật (`localhost:3001/chat`):
  1. Đăng nhập, bấm "Đổi sang DevOps" (role = DEVOPS, xác nhận qua badge sidebar). 2. Vào tab "Ask". 3. Gõ `/report` → **PASS**. Kết quả chỉ liệt kê "DevOps Engineer" (chính người đang đăng nhập), "Tổng số nhân sự: 1" — không còn thấy "Bảo Dương 2005", "Bao Duong 98", hay "Dương Nguyễn Bảo (Leader)" như vòng 1. Test thêm câu hỏi tự nhiên "Tình hình phân bổ Effort của team theo từng dự án như thế nào?" ở cùng tab/role → cũng **PASS**, không có tên thành viên thật nào khác bị lộ (model trả lời với vài vai trò chung chung "Kỹ sư phát triển"/"Kỹ sư thử nghiệm" — không khớp DB nhưng không phải dữ liệu rò rỉ, chỉ là câu trả lời chung chung của model, không vi phạm AC-CHAT-10 vì không có % effort/task thật của member khác).
  2. Đối chứng: đổi lại vai trò Leader, vẫn ở tab "Ask", gõ `/report` → **PASS**, kết quả đầy đủ như cũ: dự án LineFX, "Bảo Dương 2005: 6%", "Bao Duong 98: 25%", "DevOps Engineer: 0%", "Dương Nguyễn Bảo: 0%" — không bị ảnh hưởng bởi fix, không có regression.
  - Xác nhận qua code: `chat-box.tsx` gửi `userRole: user?.role` kèm request `/api/ai/answer-query`; route dùng `effectiveRole = userRole || currentMode` thay cho `currentMode` trực tiếp ở cả 2 chỗ áp dụng F-08 — khớp đúng "Cách fix" mô tả.
  - Không có console error trong toàn bộ phiên test (chỉ có `[Fast Refresh]` log vô hại do dev server).
  - **Kết luận**: cả 2 case (chặn đúng khi DevOps + tab Ask, không phá vỡ khi Leader + tab Ask) đều đã fix đúng. Đóng issue.
- **Cách fix**: tách đúng 2 khái niệm như gợi ý QC, không đổi hành vi tab Log/Plan vs Ask. `mode` trong `chat-box.tsx`/`ModeToggle`/request body giữ nguyên nhiệm vụ cũ (chọn route `format-entry` vs `answer-query`, chọn slash-command nào hiện, lưu lịch sử theo tab). Thêm field mới `userRole` (lấy từ `user?.role` trong `useAuthStore` — đây chính là vai trò thật đang đăng nhập, được cập nhật qua `setRole` khi bấm "Đổi sang DevOps"/"Đổi sang Leader", độc lập với tab nào đang mở) gửi kèm khi gọi `/api/ai/answer-query`. Trong `app/api/ai/answer-query/route.ts`, thêm biến `effectiveRole = userRole || currentMode` (fallback về `mode` nếu client cũ không gửi `userRole`, tránh breaking change), dùng `effectiveRole === "devops"` thay cho `currentMode === "devops"` ở cả điều kiện kích hoạt F-08 (`scopeSnapshotToMember`) lẫn khi tính `currentMemberId` mặc định — nhờ vậy dù người dùng đang ở tab "Ask" (mode gửi lên vẫn là "leader" để giữ đúng route/lịch sử), F-08 vẫn thu hẹp dữ liệu đúng theo vai trò DevOps thật. Không cần sửa gì ở `format-entry/route.ts` vì route đó không có khái niệm "xem dữ liệu toàn team" (luôn chỉ tạo entry từ input của chính người gõ).
- **Mô tả**: `components/chat/mode-toggle.tsx` định nghĩa 2 tab bằng `SegmentedControlItem value="devops" label="Log / Plan"` và `value="leader" label="Ask"` — giá trị `mode` này chính là field gửi lên `/api/ai/answer-query` làm căn cứ áp dụng `scopeSnapshotToMember` (F-08). Vì vậy bất kể người dùng đang đăng nhập với vai trò Leader hay DevOps, hễ chọn tab **"Ask"** thì `mode` luôn là `"leader"` — code F-08 trong `answer-query/route.ts` chỉ thu hẹp dữ liệu `if (currentMode === "devops")`, nên tab "Ask" không bao giờ kích hoạt nhánh này, kể cả khi người dùng thật sự đang ở vai trò DevOps. Một tài khoản DevOps chỉ cần vào tab "Ask" (thay vì "Log / Plan") để hỏi `/report` hoặc bất kỳ câu hỏi tổng hợp nào là thấy được toàn bộ effort/task của mọi member khác — đúng lỗ hổng mà FB-CHAT-03/F-08 được viết ra để chặn. Đã verify UI hiển thị rõ nhãn "Lệnh nhanh Slash (/) • **Chế độ Leader**" ngay cả khi tài khoản đang ở vai trò DevOps (đã bấm "Đổi sang DevOps" trước đó), xác nhận đây không phải nhầm lẫn thao tác mà là hành vi cố định của tab.
- **Bước tái hiện**: 1. Đăng nhập, bấm "Đổi sang DevOps" (role hiện tại = DevOps). 2. Vào `/chat`, chọn tab **"Ask"** (không phải "Log / Plan"). 3. Gõ `/report` (hoặc bất kỳ câu hỏi tổng hợp effort toàn team nào, ví dụ "Tình hình phân bổ Effort của team theo từng dự án như thế nào?"). 4. Gửi.
- **Expected**: Câu trả lời không chứa tên/effort/task của member khác ngoài chính DevOps đang đăng nhập (theo AC-CHAT-10), hoặc bị từ chối lịch sự.
- **Actual**: Câu trả lời liệt kê đầy đủ **cả 4 member** kèm % effort riêng từng người: "DevOps Engineer" (0%), "Dương Nguyễn Bảo" — Leader (0%), "Bảo Dương 2005" (6%), "Bao Duong 98" (25%) — đúng dữ liệu toàn team như khi hỏi ở vai trò Leader, không có sự khác biệt nào.
- **Đối chứng (đã verify KHÔNG lỗi)**: Cùng tài khoản DevOps, chuyển sang tab **"Log / Plan"** (mode=devops thật), hỏi câu tương tự "Ai trong team đang rảnh việc có thể nhận thêm task?" → trả lời chỉ nhắc tới "DevOps Engineer" (0% effort — đúng chính người/slot đang dùng), không lộ "Bảo Dương 2005"/"Bao Duong 98"/Leader — xác nhận cơ chế `scopeSnapshotToMember` trong `answer-query/route.ts` tự nó hoạt động đúng, vấn đề nằm hoàn toàn ở việc tab "Ask" không bao giờ gửi `mode=devops` lên server.
- **Gợi ý fix**: `ModeToggle`/`chat-box.tsx` cần gửi `mode` phản ánh đúng **vai trò đang đăng nhập** (`user.role`) khi gọi API, tách biệt khỏi khái niệm "tab hội thoại nào đang mở" (Log/Plan để tạo task vs Ask để hỏi đáp) — hiện 2 khái niệm này đang dùng chung 1 field `mode` nên xung đột. Có thể cần 2 field riêng: 1 cho "loại hành động" (log/plan vs ask) dùng để quyết định route (`format-entry` vs `answer-query`) và slash-command nào hiện ra, 1 cho "vai trò thực" dùng để quyết định `currentMode` trong `scopeSnapshotToMember`.

## ISSUE-05: Nút "Xem team đang trễ" không lọc tab "By Project" (ProjectAllocationGrid)
- **AC liên quan**: AC-03-1
- **Severity**: major
- **Status**: closed
- **QC verify (vòng 3)**: Trên Dashboard tab "By Project", bật "Xem team đang trễ" (chỉ Huy Nguyen + Linh Tran đang trễ) → task "setup môi trường" (Bao) và "Evaluate Grafana Cloud vs self-hosted" (Mai) đổi nhãn assignee thành "Unassigned", nhất quán với tab Matrix cùng lúc chỉ hiện 2 member Huy/Linh. Reset về lại đầy đủ bình thường. Không có console error.
- **Cách fix**: `app/dashboard/page.tsx` — đổi prop truyền vào `<ProjectAllocationGrid members={...}>` từ `members` (danh sách đầy đủ) sang `filteredMembers` (đã áp dụng Search/Project/Bandwidth/"Xem team đang trễ"), nhất quán với `WorkloadMatrix`/`TeamTimelineChart`/tab Cards. Không sửa `components/dashboard/project-allocation-grid.tsx` vì component đã nhận đúng member list qua prop, chỉ là caller truyền sai biến.
- **Mô tả**: Spec yêu cầu filter "Xem team đang trễ" áp dụng ở mọi tab Matrix/Timeline/Projects/Cards. Thực tế `app/dashboard/page.tsx` truyền `members={members}` (danh sách đầy đủ, không phải `filteredMembers`) vào `<ProjectAllocationGrid>` khi `activeTab === "projects"`, trong khi Matrix/Timeline/Cards đều dùng đúng `filteredMembers`. Do đó bật/tắt "Xem team đang trễ" (hoặc bất kỳ filter Search/Project/Bandwidth nào khác) không có tác dụng gì lên nội dung "Assigned DevOps" hiển thị trong mỗi project card ở tab "By Project" — luôn hiện toàn bộ member được assign, kể cả khi họ không nằm trong tập được lọc.
- **Bằng chứng**: Bật "Xem team đang trễ" (chỉ Huy Nguyen + Linh Tran đang trễ) rồi chuyển sang tab "By Project" → project card "Atlas Migration" vẫn hiện "Assigned DevOps (1): Linh Tran" và "Phoenix CI/CD" vẫn hiện "Assigned DevOps (1): Huy Nguyen" — với dữ liệu hiện tại 2 kết quả này trùng ngẫu nhiên với danh sách đã lọc (vì mỗi project chỉ có 1 member active task), nhưng khi tắt filter (Reset) rồi bật lại, nội dung "By Project" hoàn toàn không đổi ở bất kỳ trạng thái filter nào — xác nhận qua code (`components/dashboard/project-allocation-grid.tsx` dòng 19-20, nhận prop `members` chứ không có cơ chế nhận `filteredMembers`) và qua so sánh trực quan trước/sau khi bật/tắt filter trên toolbar (Project="All" so với Project="SBI", nội dung mỗi card độc lập, không đổi theo filter Project/Bandwidth/Search/Overdue nào).
- **Bước tái hiện**: 1. Vào Dashboard, chuyển tab "By Project". 2. Ghi nhận "Assigned DevOps" của từng project card. 3. Bấm "Xem team đang trễ". 4. Assigned DevOps của từng card không thay đổi theo filter (dùng project có ≥2 member assigned sẽ thấy rõ nhất; với dataset hiện tại mỗi project chỉ có 1 member nên cần đối chiếu qua code hoặc test với dataset có nhiều member/project để thấy khác biệt rõ ràng — QC đã xác nhận qua đọc source code `members={members}` là bằng chứng độc lập với dataset).
- **Expected**: "Assigned DevOps" trong mỗi project card ở tab "By Project" chỉ hiện member nằm trong `filteredMembers` (đã áp dụng Search/Project/Bandwidth/Xem team đang trễ), giống cách Matrix/Timeline/Cards đang làm.
- **Actual**: Luôn hiện toàn bộ member được assign vào project đó, bỏ qua mọi filter trên toolbar.

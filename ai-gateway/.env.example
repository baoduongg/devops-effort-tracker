# Port the FastAPI gateway listens on
AI_GATEWAY_PORT=8001

# Optional: name of the browser to read Google session cookies from
# when multiple browsers on this machine have a logged-in Google session
# (e.g. "chrome", "firefox"). Leave unset to let gemini_webapi auto-detect.
GEMINI_BROWSER=
